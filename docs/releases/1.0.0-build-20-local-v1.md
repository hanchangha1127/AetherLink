# AetherLink 1.0.0 Build 20 Local V1 Qualification Record

Status: superseded local release-engineering candidate, not a production release.

Release ID: `aetherlink-1.0.0+20-local-v1`

Build 20 is retained as an immutable historical qualification record. It binds
its non-security V1 product sources to an independently readable release
archive, retains the packaged Android, dependency, drawer-search, and
Runtime-chat boundaries, and preserves its own same-host clean-HOME and local
DMG observations. Build 22 is the current local qualification; no Build 20
observation is relabeled as evidence for a later build.

## Artifact Identity

| Item | Size | SHA-256 |
| --- | ---: | --- |
| Canonical ZIP | 165,617,269 bytes | `cba5a6531c35725aef7a2a3bf8b25d2155833b31b216906c80f8349249f6edf1` |
| External/embedded manifest | 12,317 bytes | `c633bf2c2ccc9d007f08e73929eed3d7f6b247d08579fa3695bcbad04348c99d` |
| Checksum sidecar | 99 bytes | `dd803b0bc3313d833b0cdd1b2044c96a0f5873496ecdae94c5a4079bb02feaed` |
| Unsigned Android APK | 9,575,138 bytes | `d9fcdcabfefddec4fd782a4466296b0590ba01d5b9484b613ec50c47b082d780` |
| Unsigned Android AAB | 10,676,638 bytes | `92aad079411273c0878c53dfacc5213fe269cefc6d1de546f19cdfc7f930ba46` |
| Android `mapping.txt` | 71,878,918 bytes | `2a2144d5220935030e2a9d58f8201d651066ae3614382986fa3b942da88f4b90` |
| Android `resources.txt` | 134,768 bytes | `002f51ef322a3849b5c4671db6bb6dd89722dfacd0a3418e465939ac406c005a` |
| macOS executable | 18,272,048 bytes | `92070b85256532b23b327fec5b6a46df2d98f2de89f85750ea6189c838197fb6` |
| macOS dSYM DWARF | 31,284,396 bytes | `091ed60e4b67ff8ce2d077c5c73d807ccbd3b48a8429d228040c15b9d2afb61b` |
| POM license catalog | 411,087 bytes | `1f97b74e794e5e2b3092cc31ce8c67f634a299989658feca597bc301b67dcda5` |
| Fixed compliance metadata | 94 bytes | `380bfb4b649035fc1ddbb1a8fa3e8da7bed97aa4910d22d557367332f87e0fdd` |
| Text license inventory | 109,725 bytes | `7bee5eee533db2b7c3ddc88c6e131287a0e641c92fa501bb8e680732da0e92c7` |
| SPDX 2.3 JSON | 252,417 bytes | `4ace1925a7dc7ef8c70849eec08d9839ccd15e7bf10eb2a58832c7f5ee8b4825` |
| Source inventory | 47,803 bytes | `cb2a13fbc7e441fbad4b5841ca30545bf38ee52f4d1e9be8dfaadfd5f892a1d4` |

The archive is at
`dist/releases/aetherlink-1.0.0+20-local-v1/` and contains 29 payload members
plus the embedded manifest, for 30 ZIP entries. Its 246-file source inventory
has snapshot digest
`22f14e60d522b2720660e41a645a3e9832dd723b8b93b147c51bbf6c9125998c`.
The manifest records `worktreeState=dirty-content-snapshot`; the source
boundary is `dirty-content-snapshot`. At qualification time, the manifest
captured HEAD and `origin/main` as
`dc5832b9f5926b44ccde87d11beb58355ecd9ece`.
The Git commit alone cannot reconstruct these release bytes. The archived
`source-files.json` inventory is the release source identity. The macOS app
and dSYM share UUID `0AD0CBED-7293-3151-84D1-9BAF07654A93`.

The exact archive member identities include:

- `android/apk/app-release-unsigned.apk`: 9,575,138 bytes,
  `d9fcdcabfefddec4fd782a4466296b0590ba01d5b9484b613ec50c47b082d780`.
- `android/bundle/app-release.aab`: 10,676,638 bytes,
  `92aad079411273c0878c53dfacc5213fe269cefc6d1de546f19cdfc7f930ba46`.
- `android/mapping/mapping.txt`: 71,878,918 bytes,
  `2a2144d5220935030e2a9d58f8201d651066ae3614382986fa3b942da88f4b90`.
- `android/mapping/resources.txt`: 134,768 bytes,
  `002f51ef322a3849b5c4671db6bb6dd89722dfacd0a3418e465939ac406c005a`.
- `macos/AetherLink.app/Contents/MacOS/AetherLink`: 18,272,048 bytes,
  `92070b85256532b23b327fec5b6a46df2d98f2de89f85750ea6189c838197fb6`.
- `macos/AetherLink.dSYM/Contents/Resources/DWARF/AetherLink`: 31,284,396
  bytes,
  `091ed60e4b67ff8ce2d077c5c73d807ccbd3b48a8429d228040c15b9d2afb61b`.
- `compliance/third-party-license-inventory-v1.json`: 411,087 bytes,
  `1f97b74e794e5e2b3092cc31ce8c67f634a299989658feca597bc301b67dcda5`.
- `compliance/release-compliance-metadata-v1.json`: 94 bytes,
  `380bfb4b649035fc1ddbb1a8fa3e8da7bed97aa4910d22d557367332f87e0fdd`.
- `compliance/THIRD_PARTY_LICENSE_INVENTORY.txt`: 109,725 bytes,
  `7bee5eee533db2b7c3ddc88c6e131287a0e641c92fa501bb8e680732da0e92c7`.
- `compliance/sbom.spdx.json`: 252,417 bytes,
  `4ace1925a7dc7ef8c70849eec08d9839ccd15e7bf10eb2a58832c7f5ee8b4825`.
- `source-files.json`: 47,803 bytes,
  `cb2a13fbc7e441fbad4b5841ca30545bf38ee52f4d1e9be8dfaadfd5f892a1d4`.

## Android Backup And Device-Transfer Contract

Build 20 retains `android:allowBackup="false"`. The packaged values are
`allowBackup=false`, `dataExtractionRules=@xml/data_extraction_rules`, and
`fullBackupContent=@xml/backup_rules`.

The legacy full-backup resource excludes `root`, `file`, `database`,
`sharedpref`, and `external`, each with `path="."`. The Android 12+ resource
contains exactly `cloud-backup` and `device-transfer`; each section excludes
those five domains plus `device_root`, `device_file`, `device_database`, and
`device_sharedpref`, again with `path="."`. No `<include>` rule is present.

The builder and independent checker bind the APK manifest fields to the
packaged resources and parse the compiled policy bodies. For the AAB, they
require `bundletool validate`, inspect the base manifest, materialize a
temporary AAB-derived universal APK, and apply the same compiled XML body
checks. Temporary readback signing material is local to the inspection
directory and is not a production signing identity.

The exact tools are `aapt2 dump xmltree + resources --no-values` and
`bundletool dump manifest`.

Build 15 is the first local release that requires both APK and AAB
backup-policy manifest readback. Build 17 adds compiled XML body readback for
both the APK and an AAB-derived universal APK. Builds 19 and 20 retain that
packaged-policy contract. This evidence does not claim that an Android vendor,
cloud service, or physical device performed backup or transfer.

## Android Drawer Search Boundary

Build 18 is the first immutable archive whose source inventory includes the
Android drawer search release inputs. Build 20 retains those inputs and the
bound no-device gate: 168 `AppNavigationTest` cases, 22 navigation-drawer
Compose cases, 15 search-related `RuntimeClientViewModelTest` cases, and the
complete 1,194-test app JVM suite. Release lint reports 0 errors and 2 SDK 37
availability warnings.

This retained source/JVM/Compose boundary does not establish physical touch,
TalkBack, provider, device, network, installation, signing, or release
behavior.

## Dependency And License Inventory

The catalog covers 350 unique Maven coordinates from all six Gradle lock
files; SwiftPM has zero external dependencies. Compliance profile
`aetherlink-release-compliance-v2` uses `schemaVersion=2`. The SPDX document
describes one AetherLink root plus 350 Maven packages and contains 692 exact
role relationships: 202 runtime, 155 build dependency, and 335 build tool
relationships.

Original POM bodies are not archived. License/NOTICE texts are not archived.
The offline checker does not re-fetch or re-parse those originals. Offline
readback proves catalog-derived SPDX/text consistency and recorded identities,
not legal completeness or advice. Every third-party `licenseConcluded` remains
`NOASSERTION`.

## Reproducibility Qualification

The reproducible package-only Swift path keeps `--jobs 1` and
`-Xswiftc -num-threads -Xswiftc 1`.

Two completed schema-v3 executions each built Android Release and macOS
Release from isolated 101- and 109-byte source roots. In both executions the
two lanes produced the same 165,617,269-byte canonical archive with SHA-256
`cba5a6531c35725aef7a2a3bf8b25d2155833b31b216906c80f8349249f6edf1`;
member bytes, member sets, and member metadata matched exactly, and
`sourceRootLengthsDiffer=true`.

The prepublication comparison-only result is
`dist/reproducibility/aetherlink-1.0.0+20-local-v1-two-root-v3-prepublication.json`.
It is 19,571 bytes with SHA-256
`ad7e9b6e5f52a76d5a65b52bab5138ad86eb019b7b89fa7ee29c51b89c7cef2c`.
It records `executionMode=comparison-only`, `attempted=false`,
`independentReadback=false`, `qualifiedArchivePublished=false`, and
`outcome=disabled-comparison-only`. It compared the two lanes but did not
publish or independently read back a release archive. Its captured
dirty-worktree overlay SHA-256 is
`0fdd31c2e1fcccb3915335b1cfc87c9a3b18c3c1b200f27463687014efc9ddba`.

The canonical publication-qualified result is
`dist/reproducibility/aetherlink-1.0.0+20-local-v1-two-root-v3.json`.
It is 20,010 bytes with SHA-256
`ca71f3ad64ea744275035891c5d41faae9778c6be4f1a6fbadac2c1cf2b59a1c`.
It records `executionMode=publish-qualified`, `alreadyMatched=false`,
`outcome=published-verified`, `qualifiedArchivePublished=true`,
`independentReadback=true`, `publishedBytesEqualLaneA=true`, and
`sourceSnapshotUnchanged=true`. It published the Build 20 archive from lane A
only after the exact two-root comparison and then independently read back the
published bytes. Its captured dirty-worktree overlay SHA-256 is
`f5d3ef4601129d5cde4595c73157d07fe89a3efd9904b3b9c6002504a4583606`.

Both records bind the 246-file release source snapshot
`22f14e60d522b2720660e41a645a3e9832dd723b8b93b147c51bbf6c9125998c`.
They are bounded same-host results under a fixed toolchain and dependency-cache
snapshot. They do not qualify arbitrary repeated runs, arbitrary roots,
cross-host builds, empty dependency caches, signed artifacts, clean-machine
installation, physical-device behavior, or production readiness.

## Runtime-Chat SQLite Contention And Cross-Process QA

Build 20 retains the production Runtime-chat SQLite store's five-second
(5,000-millisecond) busy timeout on every opened database connection. SQLite
`BUSY` and `LOCKED` results, including extended result codes, resolve to the
stable user-facing message `Runtime chat history is temporarily busy. Try again.`
without a database path or raw SQLite lock text.

Three deterministic Swift tests control transaction progress with explicit
wait points around `BEGIN IMMEDIATE`, writes, and `COMMIT`:

- a second store instance waits for the first writer and then commits its
  event exactly once;
- a writer held beyond five seconds fails with the stable busy message, leaves
  no contender event or FTS row, and a later reopened store succeeds;
- a read transaction that blocks `COMMIT` forces rollback of both the event
  and FTS row before the same event succeeds after reopen.

Those three deterministic wait/`BEGIN`/`COMMIT` tests remain included within
the passing 90/90 `SQLiteRuntimeChatEventStoreTests` suite.

The QA-only Swift executable source retained by Build 20 is
`apps/macos/RuntimeChatSQLiteCrossProcessQA/Sources/RuntimeChatSQLiteCrossProcessQA.swift`,
11,570 bytes with SHA-256
`137bcf0cb948f2d82718ef8c6df52147be5a9f713dd6743bc377a9940bece951`.
It links `CompanionCore`, accepts one exact temporary database root, and is not
shipped in the app. The Python runner is
`script/run_macos_runtime_chat_cross_process_smoke.py`, 26,948 bytes with
SHA-256
`d5a7b11cbed4a0e04f3617a8cce7f69b09f285c95c5825f7ea7f415a811fde53`.
Its 13,950-byte test source has SHA-256
`2a9d1add3ac6343aeeeb0b746f1182dbba148321f214a74211fe59b19e888e61`.
The bounded Python orchestration suite passes 12/12 tests, including oversized
writer/readback output and process-group timeout cleanup regressions.

Build 19 recorded one same-host live fixture with two independent helper
processes; each writer appended 48 deterministic events through the production
store. A third-process readback used a fresh helper process. The observed
`48+48=96` event sets were disjoint and exactly once, per-writer order and
owner/session isolation were preserved, and SQLite integrity and foreign-key
checks passed. The temporary root/database permissions were `0700` and `0600`.
This live result is separate execution evidence, not an archive member.
Build 20 source-binds the unchanged helper and runner but does not
relabel the prior live execution or claim a new cross-process live run.

The retained fixture does not qualify crash or power-loss recovery, old/new
binary coexistence or migration, a clean machine, signed or notarized
distribution, physical-device behavior, network/provider behavior, arbitrary
workloads, or production readiness. It adds no security qualification claim.

## Historical Build 19 Boundary

Build 19 remains frozen as `aetherlink-1.0.0+19-local-v1`: 165,617,111 bytes,
SHA-256
`d792b3ab39a32b0076c97e206c773845cb1081a477ae1f309395c91699432ec8`.
Its 12,317-byte manifest SHA-256 remains
`7379a910bf055813d02bc0ec9810dfee3443655d6c6e4d0f30e548a0cbe2a99a`,
and its 245-file source snapshot remains
`1f38043525dc622bf839137e5da9bbcd4d3731403b3c32b62508da465655ffc5`.

Its clean-HOME installed-app observation remains frozen at
`dist/lifecycle/macos-packaged-app-build-19-clean-home-install-v1.json`,
2,250 bytes with SHA-256
`a89291227bde1f9f15caa3743339f569e9f7c79380f8f3a70df0a0fe8388b159`.
Its installed state-recovery observation remains frozen at
`dist/lifecycle/macos-packaged-app-build-19-clean-home-state-recovery-v1.json`,
3,364 bytes with SHA-256
`1c72536188ce71388319d068489f4c351521f33d5431af36e7acc5ff76bdb2b7`.
Those observations and the Build 19 Runtime-chat live fixture remain bound only
to Build 19. Build 20 does not alter, relabel, or retroactively extend them.

## Historical Build 20 Clean-HOME Installed Lifecycle Evidence

<!-- aetherlink-historical-build20-lifecycle-v1:start -->

Build 20 has a same-host, per-user clean-HOME installed-app observation at
`dist/lifecycle/macos-packaged-app-build-20-clean-home-install-v1.json`,
2,250 bytes with SHA-256
`4ce047a318e47568d647e1167cbaeebc603626073e098451a29c949086aa3d72`.
Its runner and test SHA-256 values are
`55441bb84a9d8e4681af558dc7a1d017333c88fcdeb6ab2a84561c0ee093ca29`
and
`55274ad4abc958d85c4df1193cfe1508d820768fbbe48eae71a4fee8c1c020aa`.
The exact packaged app was copied with `ditto` into an isolated per-user
Applications path. Two distinct exact-path LaunchServices processes reached
regular AppKit activation, completed their observations, and accepted
identity-bound termination. The three initialized SQLite files passed
integrity readback, Runtime chat remained empty, and the installed state bytes
and modes stayed unchanged across relaunch.

Build 20 also has an installed state-recovery observation at
`dist/lifecycle/macos-packaged-app-build-20-clean-home-state-recovery-v1.json`,
3,364 bytes with SHA-256
`d12947e16e7b985515a90a13731947a5991bcd82a06039210e22bba43535bf0b`.
Its runner and test SHA-256 values are
`9d05896a5dcce7e3d7642b41acba2ee4bf6d28ffda85bc8f3b2b645f2a3b273a`
and
`edfd6f89b2cecd6de5cbfcb337ba6f5643a8d74d7caf8735c467578488970664`.
The first process migrated the fixed legacy JSONL canary into one exact
Runtime-chat SQLite row. After termination and legacy removal, a distinct
second process recovered the same row from SQLite-only state. The auxiliary
databases passed integrity checks and the installed app and remaining state
bytes and modes stayed unchanged.
Both clean-HOME runners were invoked twice and matched their canonical
results.

These historical same-host, per-user Build 20 lifecycle observations do not themselves
qualify a clean machine/account, DMG/Finder installation, signed/notarized
distribution, UI/accessibility, live-provider behavior, a physical device,
arbitrary histories, crash/power-loss, concurrent writers, backup/transfer,
rollback, or production readiness. The separate local DMG observation below
does not broaden these two clean-HOME results.

<!-- aetherlink-historical-build20-lifecycle-v1:end -->

## Historical Build 20 Local DMG Mount-And-Copy Lifecycle Evidence

Build 20 has a bounded same-host, per-user command-driven local DMG observation
at
`dist/lifecycle/macos-packaged-app-build-20-local-dmg-install-v1.json`.
It is 2,434 bytes with SHA-256
`e78b605278d5c5b7f5601778c38f35270f1db4a9e95055ff434b71af4c33cf78`.
The 32,324-byte runner
`script/run_macos_local_dmg_install_smoke.py` has SHA-256
`e082ce1aaf7f65bfb63bb2b5fd58136af1510eb6d1689faa1014c018b74129fb`;
its 61,144-byte test source has SHA-256
`89e566ff26d22eced043ffa108f8719274f9608685d6ecd18e579291c021cf47`.

The runner generated a verified ephemeral UDZO HFS+ image containing the exact
release app and an Applications alias. It mounted exactly one image entity at
an exact fresh, read-only, `nobrowse` mountpoint; copied the exact release tree
with `ditto` into an isolated per-user Applications directory; verified the
ad-hoc app seal and version; unmounted and detached the image before launch;
and did not retain the image. Two distinct LaunchServices processes launched
the exact installed bundle in separate cycles, reached regular AppKit
activation, completed their observations, and accepted termination. The three
SQLite files passed integrity checks, Runtime chat remained empty, and state
bytes and modes stayed unchanged across relaunch.

This observation is not Finder UI or drag-and-drop evidence, general UI or
accessibility evidence, Developer ID/notarized/stapled distribution evidence,
Gatekeeper quarantine or download evidence, or a clean-machine/account or
system Applications installation. It does not cover TCC, Keychain, providers,
networks, devices, arbitrary history, crash/power-loss, concurrent writers,
backup/restore, device transfer, upgrade, N/N-1, rollback, production
readiness, or security qualification.

## Compatibility And Transition Boundary

- Android Release and macOS package metadata use `1.0.0+20`. Android Debug
  `0.1.0+1` remains a separate, non-migratable development lineage.
- Android is unsigned, `arm64-v8a`-only, minimum API 26, and target API 36.
  The macOS package is local ad-hoc, Apple Silicon `arm64`, and macOS 14+.
- There is no prior production predecessor. N/N-1 and old/new binary
  compatibility are not yet qualified.
- The local DMG result covers only the exact command-driven same-host
  mount/copy/two-launch path above. It does not claim Finder installation,
  downloaded-image handling, a signed/notarized channel, or a clean machine.
- Production signing, notarization, store upload, deployment, provider-backed
  Runtime behavior, and physical-device execution are not claimed.
- This local result does not complete the full G6 exit or production
  readiness. It does not add a security qualification claim.

## Rollback Posture

No production rollback package is claimed. Android channel rollback must use a
compatible semantically older build with a higher `versionCode`; ordinary
downgrade installation is not a supported recovery path. A future macOS
rollback should use the current or immediately previous signed DMG only after
compatibility and migration checks, not this local ad-hoc archive or its
ephemeral local DMG observation.

## Independent Readback

Run this historical source-bound readback with historical mode:

```sh
python3 -B script/check_release_artifact_archive.py --archive-dir dist/releases/aetherlink-1.0.0+20-local-v1 --historical
```

Build 19 and older archives also require historical mode.

Focused non-security verification:

```sh
python3 -B -m unittest \
  script.test_release_version_ledger \
  script.test_release_artifact_archive \
  script.test_run_clean_release_reproducibility \
  script.test_run_macos_clean_home_installed_app_smoke \
  script.test_run_macos_clean_home_installed_state_recovery_smoke \
  script.test_run_macos_local_dmg_install_smoke \
  script.test_documentation_handoff_guards
git diff --check
```

No staging, commit, or push is part of this qualification.
