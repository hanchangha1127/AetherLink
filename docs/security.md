# AetherLink Security Model

## Production P2P/NAT libjuice Source Audit Rejection

The exact official-tag libjuice v1.7.2 source and Android NDK r28c were acquired under the bounded one-shot decision-v2/handoff-v5 authority. The source manifest proves that all 81 extracted regular files match the retained archive and records the license, embedded notices, generated-file result, dependency closure, build inputs, NDK/tool hashes, and Apple tool metadata.

Static audit rejects the candidate before compilation. Android and macOS use a realtime-seeded fallback for ICE credentials, tie-breakers, and transaction IDs; a remote integrity failure can log the active ICE password at the default WARN level; unauthenticated Binding errors can alter role or pair state; unauthenticated TURN errors can apply an ALTERNATE-SERVER destination; and string endpoint/getaddrinfo paths cannot enforce immutable numeric tuple authorization before DNS and socket boundaries. These are internal source/API failures and cannot be repaired by the proposed wrapper alone.

The failure policy closes libjuice and opens a versioned libnice review only. `libnice-0.1.23-glib-c-abi` is not selected or acquired. Decision-v3 and handoff-v6 close additional acquisition, source fork, compiler/archive, source execution, socket, runtime/harness network, Phase B, production network, and deployment authority. No Android/macOS compilation or runtime protocol evidence exists.

## Resource-Bounded Provider Model-Catalog Trust Boundary

Provider model catalogs are untrusted input even when they come from localhost. Ollama `/api/tags`, `/api/ps`, and `/api/show` and both LM Studio catalog shapes use a true streaming 4 MiB response ceiling: an oversized positive `Content-Length` fails before body ingestion, and an unknown or inaccurate length stops at the first byte above the limit. They then reject malformed JSON plus duplicate or escape-equivalent object names before decoding. Catalog row count is capped at 256, Ollama detail fanout is capped at the same 256 unique identities and propagates cancellation, model identity/display/remote fields are capped at 512 Unicode code points, provider-qualified identities are capped at 522, and capability metadata is capped at 32 byte-exact-after-decoding unique content-bearing values of at most 128 code points each. NFC/NFD-distinct capability values remain distinct. Swift, Android, and schema use the same explicit blank/invisible code-point set instead of platform whitespace APIs. LM Studio additionally caps each native `loaded_instances` array at 256 unique nonblank identifiers of at most 512 code points before any unload POST and revalidates it during every poll. Every excess fails closed without truncation. Ambiguous catalog identity also fails closed: Ollama rejects exact and canonical `:latest` duplicates and requires `name`/`model` agreement; LM Studio rejects exact and NFC-equivalent duplicates and requires byte-exact `key`/`id` agreement.

Context-window metadata becomes trusted only after provider numeric fields decode as `Decimal` and `ModelInfo.validatedContextWindowTokens(decimal:)` uses `NSDecimalRound` plus exact integral equality to prove every present alias is mathematically integral, positive, no greater than 16,777,216, and mutually equal. This rejects `16777215.9999999999`, `16777216.0000000001`, and precision-rounded alias bypasses while retaining exact integral decimal and scientific forms. Size remains an exact integer in 0...`Int64.max`; the router uses integer JSON encoding rather than a lossy `Double`. A structurally malformed, oversized, or duplicate-key Ollama show response excludes the affected model; if only context metadata is invalid or conflicting, the context value is omitted while validated capabilities survive. LM Studio may try the OpenAI-compatible catalog only after native HTTP 404, 405, or 501, never after oversized or malformed data, transport/auth failure, or other 4xx/5xx status. Native chat may fall back only after an explicit pre-stream HTTP incompatibility response; malformed partial output and clean EOF after a delta both terminate without a second provider dispatch because successful native streaming requires explicit `chat.end` in the main loop or from `parser.finish()`. The router revalidates the final row and metadata bounds and encodes the full response before publication; plaintext above 1,048,560 bytes is rejected so relay authentication cannot exceed the 1 MiB frame. An injected non-nil invalid value or oversized aggregate rejects the entire catalog instead of exposing a partial result. A genuinely absent provider context value still leaves compaction on the conservative legacy fallback.

Public `models.list` dispatch is also resource bounded across callers. Eight concurrent public waiters may share one provider scan; a ninth gets a fixed retryable `backend_unavailable` without provider details. Per-waiter continuations let a canceled non-last request return immediately while remaining callers keep the shared operation. Last-waiter cancellation stops the provider Task, and that Task must retire before replacement, preventing cancel-and-reissue churn from overlapping provider scans. Success and failure are not cached. Publication still rechecks each waiter's own request task, authentication generation and session, transport binding, and current trusted-device key. Internal authority catalog lookups are deliberately not coalesced with the public path.

This tightens validation of the existing wire shape but changes neither wire authority nor execution authority. It adds no protocol field, Android action, provider route, socket/network permission, controlled-spike Phase B, production networking, or deployment authority. URLProtocol/unit and no-device Android tests establish parser and routing behavior only, not physical-device, live-provider, peer-receipt, or external-network behavior.

## Memory-Summary Durable Terminal Decision Linearization

- Approval and dismissal are one owner-and-draft-scoped terminal state in the production JSONL memory store. Both paths hold the canonical event-log path lock plus the OS coordination-file write lock while rereading history in physical append order, classifying prior source-bound approval and dismissal records, and conditionally appending. Two independently opened stores or processes cannot commit both outcomes, and wall-clock rollback cannot reorder a later mutation ahead of the event it followed.
- Same-action retries are read-only. Dismiss retry returns the first physically persisted dismissal timestamp. Approval retry returns the current matching entry, preserving any later user content edit or disabled state; deletion or provenance replacement returns unavailable and never resurrects the approved content.
- Opposite decisions and contradictory legacy terminal history fail closed as `memory_summary_draft_unavailable`. Generic memory upsert is no longer used by the router approval path. Stores without explicit atomic support fail closed, and other persistence failures become sanitized `memory_store_unavailable` responses rather than raw internal errors.
- The default no-device gate and copy hygiene pin the atomic store API, both file-lock spans, conflict mapping, in-process and separate-process approve-versus-dismiss races, clock-rollback edit/delete preservation, opposite-decision rejection, dismissal retry, sanitized router errors, and all five documentation surfaces.

## Memory-Summary Source-Bound Review Identity And Commit Linearization

- Long-inactivity review drafts use opaque `long-inactivity:v2:<64 lowercase hex>` ids derived with a domain-separated, length-framed SHA-256 input. The identity binds session id, title, model, exact last activity, session message count, selected source count/range, every ordered pointer field, and deterministic preview. It intentionally excludes only naturally advancing inactivity duration.
- Approval and dismissal do not rely on the earlier lookup as final authority. The exact recomputed base draft must still match inside the chat store's source-current transaction, and that transaction spans the actual memory-store upsert or dismissal. A concurrent rename, transcript update, model change, archive/delete transition, or pointer/preview change therefore wins before the decision and makes it stale, waits behind the same store, or fails SQLite locked/busy through a separate writer until the authorized decision has committed.
- Existing v1 approval, dismissal, and generated records are never promoted into current v2 authority. Because the weak historical id cannot prove title/model/source-pointer/preview identity, it cannot suppress or overlay a v2 draft. Draft existence and stale guards precede installed-model resolution, so v1 generate, approve, and dismiss requests fail before provider model-catalog access, backend dispatch, or memory mutation.
- The default no-device gate and copy hygiene pin exact hash inputs, the inactivity exception, SQLite reopen stability, both commit races, both independent-store actual-mutation lock spans, operation-error propagation, pre-model-lookup v1 rejection and stale guards, pre-dispatch rename rejection, and the full legacy transition matrix. Focused selections pass 15 and 23 checks, and all 61 memory-summary router checks pass.
- The first aggregate exposed a smoke-fixture title race: automatic title generation may commit after `chat.done`, and title is intentionally bound into the v2 id. The old review therefore failed closed as unavailable. The corrected relay fixture acknowledges a stable explicit rename before listing, while a router regression proves a later title change is rejected before provider model lookup. Final GPT-5.6 Sol re-review reports `no P0-P3 findings`.
- All 1,484 current-source Swift tests pass with zero failures in 282.207 seconds. `build/qa/check-no-device-quality-memory-summary-source-bound-review-identity-final-reviewed-r2-20260716.log` exits 0 across 11,605 lines with one overall marker, one dedicated marker, two zero-delay registry self-tests, 56 authenticated relay connections, and 905 encrypted frame bodies. `adb devices -l` reports no attached device.
- This is no-device deterministic SwiftPM/temporary-store evidence. It is not physical Android, optical QR, peer receipt, live-provider, external-network, production relay/P2P, NAT traversal, measurement, or deployment proof and adds no protocol, capability, permission, source, provider, Python/process/file/socket/network, Phase B, or production authority.

## Android Memory-Summary Request Deadline Closure

- Production-created Android ViewModels bind memory-summary list, approve, and dismiss control requests to 15-second Jobs and generate requests to 75-second Jobs, all keyed by request id. The host's 60-second whole-generation deadline remains authoritative; the extra 15-second client margin allows its timeout/error terminal to arrive. Timeout authority additionally requires the exact pending channel object, connection generation, and authenticated runtime-authority generation, so a stale Job cannot close a replacement operation.
- A deadline removes only its request's pending and UI action state. Concurrent action timeouts preserve the reconciliation barrier, and the last timed-out action drains one deferred list refresh exactly once. Late result/error frames after timeout are inert: they cannot mutate review or durable memory, replace the timeout error, trigger another refresh, or revoke authentication.
- Every normal result, protocol error, malformed terminal, and send-failure completion cancels the exact deadline before closing. Reauthentication, revocation, disconnect, receive failure, and ViewModel clear cancel the complete Job collection and correlations.
- A generation result at 59,999 milliseconds remains valid after the control deadline and cancels its exact retained Job. Timeout-enabled protocol error, malformed result, and send failure paths likewise cancel their exact retained Jobs. Late result/error frames after timeout are inert, and the last action timeout drains a deferred refresh exactly once.
- GPT-5.6 Sol found no product-code defect and no P0-P2 finding; its P3 test-evidence gap for distinguishing Job removal from cancellation is remediated with direct `isCancelled` assertions.
- All six focused tests pass under Android Studio JBR, including Robolectric execution of the production factory. The 35-test memory-summary/factory selection, all 575 ViewModel tests, and all 1,049 app unit tests pass with zero failures, errors, or skips. Final GPT-5.6 Sol re-review reports `no P0-P3 findings`.
- `build/qa/check-no-device-quality-android-memory-summary-request-deadline-final-reviewed-r2-20260716.log` exits 0 across 11,556 lines with one deadline addendum, 88 local development-relay matches, 903 encrypted frame bodies, and two zero-delay mock registry self-tests. This remains no-device evidence and opens no production network authority.
- This is no-device Android JVM state-machine evidence, not physical-device, optical QR, peer-receipt, live-provider, external-network, production P2P/relay, NAT traversal, measurement, or deployment proof. It opens no protocol, permission, source, process, Python, socket/network, Phase B, production, or deployment authority.

## Android Memory-Summary Terminal Channel And Source-Identity Closure

- Android accepts a memory-summary list, generate, approve, or dismiss terminal only when request id, source channel object identity, connection generation, and authenticated runtime-authority generation all match the pending correlation. Wrong-channel and late-duplicate result/error frames are inert: they cannot consume pending state, mutate UI or persisted memory, trigger reconciliation, surface an error, or revoke authentication.
- Generated and approved provenance binds draft id; session id, title, model, last activity, and message count; source count and range; and exact source-pointer count, order, session, index, role, timestamp, and excerpt. Approved provenance also binds source kind and summary method. Only `inactive_seconds` is excluded from source identity because it advances with wall time. Generated summary method, timestamp, and model remain subject to their separate closed result-metadata checks.
- The default no-device gate and copy hygiene pin both field-mutation and channel/replay regressions. This is Android JVM state-machine evidence, not physical-device, peer-receipt, live-provider, external-network, production relay/P2P, NAT traversal, measurement, or deployment proof.
- The final Android class passes 570 checks, GPT-5.6 Sol reports no P0-P3 finding, and `build/qa/check-no-device-quality-android-memory-summary-terminal-source-closure-final-reviewed-20260716.log` exits 0 with one dedicated marker. `adb devices -l` reported no device, so this remains no-device evidence.
- No source, permission, provider, process, Python, socket/network, Phase B, production, or deployment authority is opened. Controlled-spike approvals remain bounded Phase A and unselected recommendations remain inactive.

## Exact Provider Dispatch Identity And Host-Approval Publication Linearization

- Runtime model resolution produces an exact provider and exact raw `provider_model_id`. Direct adapters receive the raw id and aggregate routing receives one canonical provider-qualified id. Aggregate chat and embedding match only the selected provider plus exact installed provider id; display aliases cannot substitute after resolution, and provider-native ids containing reserved provider qualification fail closed.
- The external requested model remains unchanged for protocol correlation and runtime-owned records. Generated memory-summary cache provenance separately stores host-local `provider_qualified_model_id`, and cache/single-flight reuse binds it with requested model and prompt revision. Legacy records without that identity remain readable but are not authorization for new reuse.
- Shared memory-summary work is waiter-owned. The worker returns only a candidate. Cancellation of the last waiter closes that flight to new waiters even after worker completion and cancels only unfinished work; delayed cleanup rechecks exact flight identity plus current retired/empty state before removal. A later request gets a fresh identity-guarded flight. The first still-authorized waiter performs the one cache materialization and its response publication while holding the request and flight authority locks, so connection close wins with neither side effect or publication wins with both completed.
- Host approval reservation is a sealed one-use capability. `invalidateAndWait` prevents a callback retained past authorization from creating a later reservation, a wrong receipt waits for concurrent reservation persistence to settle, and explicit persistence results distinguish durable expiry terminalization from an unproven error. Audit time is never earlier than the actual reservation commit.
- The action adapter does not own terminal publication. It may only `prepareOutcomePublication`; the coordinator supplies that closure a one-use terminal-commit capability. Every `RuntimeMessageSink` must provide a transport-context transaction. The relay sink's dedicated context lock serializes current binding validation, exact lifecycle/authentication/trust/permission checks, terminal commit, and send against context mutation. The terminal gate rechecks both deadlines and durably records the exact success/failure terminal or suppression. Stale authority or transport, preparation failure, deadline crossing, duplicate commit use, invalid receipt use, or ambiguous durability sends no wire result and may place the coordinator into storage-degraded recovery.
- Focused no-device tests cover exact model identity, provider-prefix ambiguity, cache/provider separation, last waiter cancellation, reservation escape/reuse, expiry proof, publication delay, clock rollback, and persistence transition parity. This does not prove a live provider, physical Android, optical QR, external network, or production transport.
- The refreshed default aggregate exits 0 in `build/qa/check-no-device-quality-v05-provider-dispatch-approval-linearization-final-reviewed-20260716.log` with one overall success marker, one dedicated marker, two zero-delay registry self-test passes, 88 local development-relay matches, and 899 encrypted frame bodies. Android SDK `adb devices -l` reported no attached device, so this remains no-device evidence.
- All four controlled-spike approvals remain bounded Phase A. Source acquisition, inspected-source execution, socket/network I/O, Phase B, production networking, and deployment remain closed.

## Host-Local Approval Review Text Anti-Spoofing

- `RuntimeApprovalReviewText` is the single projection for requesting-device text shown in the existing macOS approval review. The authenticated router projects the current trusted-device record, the `models_pull_ollama_v1` adapter repeats the idempotent projection, and the host coordinator rejects noncanonical review text before persistence or publication.
- Safe output is re-normalized, re-trimmed, and iterated to a bounded fixed point after filtering and 512-byte `Character` bounding, then must pass the same projection unchanged. Controls/newlines, bidi formatting/isolate marks, private-use/unassigned and braille-blank scalars, and noncontextual default-ignorable code points are removed. ZWNJ requires either canonical `Virama` or Unicode `Joining_Type` context, ZWJ requires either canonical `Virama` or `Extended_Pictographic` context, emoji/text and CJK ideographic variation selectors require valid bases, and only RGI England/Scotland/Wales subdivision tag sequences remain intact. Approved tag positions are precomputed once with a linear scan to prevent single-grapheme tag-run amplification. Invisible-only input renders as `Trusted device`.
- The permission claim derives an uppercase six-byte display fingerprint from the authority public key already included in its binding digest. The coordinator requires exact fingerprint equality with the claim, and the UI renders it beside the safe device name. A host-owned FSI/PDI pair isolates the user-controlled name from surrounding localized text, including ordinary RTL input.
- The projected name and short comparison fingerprint are ephemeral and non-authorizing. Neither can change the action, policy revision, authenticated device/key binding, model resource, durable reservation, or publication authority. Raw display bytes and the fingerprint are not added to the redacted audit or protocol wire result, and provider dispatch remains impossible before explicit host approval.
- Final no-device verification passes the 40-test focused selector, all 1,363 Swift tests, the five-language light/dark render matrix, both GPT-5.6 Sol reviews with no remaining P0-P3 findings, and the default aggregate gate. This remains synthetic/local evidence and does not replace assistive-technology or physical-device review.
- This does not activate a generic permission surface, mobile approval, second action, Python/file/process/terminal/network/MCP/web capability, source acquisition, socket execution, or production transport. The focused proof is no-device Swift and macOS render evidence only, not physical Android, optical QR, live-provider, external network, production P2P/relay, or deployment evidence.

## Citation And Device Trusted-Source Review

- A citation is an opaque runtime-issued handle over the current `runtime_shared` source revision and source anchor. Its wire envelope exposes only safe document metadata and a bounded chunk summary. Source revision, approval id, path, body, snippet, query, model, vector, cache, backend, workspace, and project fields are excluded.
- `citation.resolve` prepares a one-time ten-minute review for the authenticated device. The confirmation token contains 256 bits read directly from the operating-system CSPRNG and encoded as hexadecimal, is stored only in the owner-only runtime SQLite database and transient Android ViewModel state, and is never persisted by Android or written to audit.
- `trusted_source.approve` creates a reusable device-owned `chat_context` grant without mutating host `runtime_shared` approval. Review token, actor identity, expiry, disclosure version, usage scope, citation revision, and current source approval are checked in one immediate transaction. Token replay, another device, expiry, or changed source fails closed.
- Same-revision reindex preserves current grants. Changed replacement, host revoke, delete, and filtered maintenance stale citations and make reviews or grants unusable before subsequent list or approval. Approval/replacement races serialize under the document-store lock and transaction, so a grant cannot survive a later revision change.
- A `chat.send` may name one through eight unique canonical grant ids only in the top-level `trusted_source_grant_ids` authorization field. One immediate transaction validates the entire set against authenticated-device ownership, current revision, host approval, citation/anchor/chunk identity, scope, and revocation state; partial success is impossible. It writes content-free `trusted_source_context_consumed` events before returning bounded text. Any audit failure blocks text release, and any stale member fails the whole request without revealing whether it belongs to another device or was revoked.
- Prepare, resolve, approve, list, dismiss, revoke, consumption, and host invalidation append content-free audit actions. The local audit ledger retains the newest 100,000 events; each insertion trims only the oldest overflow in the same transaction. Audit and export contain safe source/revision identity, authenticated actor, source anchor, result count, action, and time, but no confirmation token, review/grant handle, query, snippet, or body.
- Android exposes only safe document metadata, trust state, and transient selected sources. Opaque ids and tokens remain private to the ViewModel and clear on disconnect, trust/session changes, authentication loss, malformed trusted-source errors, terminal review/revoke errors, or list invalidation. Grant ids enter only the encrypted top-level runtime authorization field and never enter device persistence, client-visible messages, accessibility text, logs, or model context. ViewModel dispatch binds envelopes to the selected channel object, and direct TCP send/receive capture the current socket before switching to the IO dispatcher, so a queued grant-bearing request cannot move onto a replacement socket of the same client object.
- Each materialized excerpt is capped at 4,096 UTF-8 bytes. The runtime removes legacy or spoofed capability guards, prepends the current canonical guard, and appends safe document name, MIME type, chunk index, and text as backend-only JSON reference data to the newest user turn. The guard says text values are reference data rather than instructions. Stored events retain the original client messages only. If mandatory context cannot fit the selected model window, the request fails with `chat_context_window_exceeded` before backend dispatch.
- Successful trusted-source answers store a separate redacted attribution snapshot containing only a one-based source index, safe document name, canonical MIME type, and chunk index. This records which reviewed source contexts the runtime provided for generation; it is not model-authored, sentence-level entailment, or current authorization. It is emitted only to clients advertising `chat.source_attributions.v1`, and authenticated history restores it without grant, citation, anchor, document id, fingerprint, revision, approval, source text, snippet, offset, path, or backend metadata. Cancellation, backend error, context-window rejection, audit failure, and requests without consumed grants do not create attributions. A later revoke does not erase historical provenance, while regenerate replaces the prior assistant answer and its attribution snapshot.
- Historical attribution review adds no authority to those four display fields. Under `chat.source_attribution.resolve.v1`, only attribution-bearing successful completion/history rows receive a server-generated `assistant_message_id`; the locator may persist but is non-authorizing. The runtime atomically stores an internal attribution binding containing only `source_index`, `source_anchor_id`, `document_id`, and `source_revision`, resolves only canonical owner-scoped history, and atomically revalidates current `runtime_shared` approval plus that exact historical revision before creating the existing one-time review. Approval state and a separate chunk identifier are not stored in the binding. Authority is never reconstructed from document name, MIME type, chunk index, or source index. Regenerated/deleted answers, legacy rows without bindings, deleted/replaced/revoked sources, stale revisions, wrong owners, and malformed state fail closed.
- Android attribution clicks reuse the existing review dialog. Only the locator may persist with history; review and confirmation tokens plus citation, grant, anchor, document, approval, and revision identifiers remain private transient state and clear under the established authentication, transport, trust, and terminal-error cleanup rules.

## Approved Semantic Document Retrieval

- The cache shares the document index SQLite database because approved documents have `runtime_shared` scope. It intentionally has no device-owner key: all authenticated trusted devices on one runtime reuse the same approved candidate vectors, while separate runtime databases remain isolated.
- Candidate keys bind document and chunk identity, the full SHA-256 approved source revision, canonical provider-qualified embedding model id, strong content-stable model fingerprint, versioned semantic-document encoding, and exact bounded-content SHA-256 fingerprint. The cache API has no query-vector record type, and no vector, model, fingerprint, revision, score, or cache state crosses the wire.
- Only the existing strong immutable local-model fingerprint policy may supply persistent keys. Canonical Ollama artifact SHA-256 revisions qualify; providers without an immutable revision remain on-demand.
- Replacement, revoke, delete, and filtered maintenance delete derived rows in the same SQLite transaction as document mutation. A slow inference write acquires `BEGIN IMMEDIATE`, revalidates current `runtime_shared` approval, source revision, and bounded chunk fingerprint, then checks cancellation again before commit. It cannot repopulate a replaced or revoked source.
- Strong-fingerprint resource ceilings are 200 approved candidates, 4,096 UTF-8 bytes per candidate, and 64 candidates/262,144 bytes per missing-vector batch. Weak/no-fingerprint providers atomically embed one query plus at most 63 candidates and do not persist vectors, preventing cross-request model revision mixing. New approvals stop at 800 `runtime_shared` sources, all remain visible and revocable through host management independently of the 100-row remote catalog page, and oversized legacy sets fail semantic reads closed. Candidate discovery selects at most 200 usable documents, reads at most 800 chunk rows total without a corpus-sized window sort, and is interruptible through a SQLite progress handler tied to task cancellation. Candidate selection round-robins across documents; cosine normalization is overflow-stable. Cache storage remains capped at 2,000 rows and 32 MiB per model and 64 MiB total.
- Optional result `match_kind` distinguishes ranking origin without inventing lexical terms. Omission is legacy lexical, while only explicit `embedding_model_id` requests receive semantic origin. Android retries one strict older-runtime unsupported-field rejection without the hint, but backend, index, approval, audit, and embedding failures never downgrade silently.
- Before candidate inference, an immediate transaction revalidates approval scope, source revision, chunk identity, model input byte limit, and bounded fingerprint and writes a content-free `semantic_accessed` audit. After inference, another immediate transaction drops stale candidates and conditionally writes content-free `.queried` before response serialization. Failure or cancellation observed before the final commit leaves the access record but no completed-query record; after commit, serialization proceeds. Neither event stores query, body, model, score, vector, or cache state.
- This no-device activation does not prove live-model quality, physical Android, optical QR, production relay/P2P, or real-network behavior. The newer section adds review/grant authorization lifecycle but not model-context consumption.

## macOS Host Document Source Review

- File selection opens security-scoped access only for a coordinated, bounded copy into a random mode-`0600` temporary snapshot. Directories and symlinks are rejected, copying stops at 32 MiB, extraction runs only against the snapshot, and cleanup occurs before the candidate review is published. No path or bookmark is persisted.
- A prepared candidate is not an approval. It remains in actor-isolated memory for at most ten minutes and requires the exact one-time token plus disclosure version. Replacement binds the candidate to the expected active source revision so a stale review cannot overwrite a newer approval; the prior revision stays readable until the confirmed atomic replacement commits.
- Every approved source has explicit `runtime_shared` scope and is available to every authenticated trusted device on that runtime. The UI states this runtime-wide boundary before approval. `CompanionAppModel` and the runtime router share one injected SQLite store, preventing a UI-only approval state from diverging from remote authorization.
- Removal requires the current source revision and atomically revokes and deletes document, chunk, FTS, and approval state. Content-free revoked/deleted tombstones remain in audit. Router document-store failures return a generic `document_index_unavailable` detail rather than forwarding storage errors that could contain host paths.
- Audit retention is a rolling newest-100,000-event local ledger, bounded identically in memory and SQLite. Preview is limited to 50 events and export to the latest 1,000. The JSON schema contains action, safe document/revision identity, authenticated actor id, source anchor id, result count, and time, but no file path, bookmark, body, query, snippet, or pending candidate content.
- Approved semantic document retrieval, device-scoped citation review, and revision-bound chat-context consumption are active under the newer sections above. Client grants still cannot modify host approval. The phone is disconnected, so physical Android, optical QR, live-provider answer quality, production relay/P2P, and real-network behavior are not proven.

## Runtime-Shared Document Source Governance

Document retrieval is runtime-wide by design in this foundation. An approval has the explicit `runtime_shared` scope, so every authenticated trusted device is an authorized reader of the same host library. The runtime does not claim per-device or per-user source isolation. Production UI must explain this sharing boundary before it exposes source approval.

The approval ledger is separate from indexed document rows. Approval binds document id, a strong SHA-256 revision over canonical safe metadata and every normalized chunk, host approver identity, and approval time. Existing rows remain unapproved and unreadable. SQLite replacement and revoke/delete transactions keep approval, document, chunk, and FTS state aligned; revoke/delete invalidates lexical results and prior anchors while retaining the bounded content-free audit history.

Remote catalog, retrieval, and anchor reads append an audit event and trim only oldest overflow beyond 100,000 rows in the same store transaction as the approved source snapshot. That commit is the authorization linearization point, so concurrent revoke cannot interleave between approval read and audit, and all later operations observe revocation. Events contain authenticated device id, action, safe source identity/revision, optional anchor id, result count, and time, but no query text, snippet, or document body. If governance, audit insertion, or retention trimming is unavailable, the transaction rolls back and the read fails closed with no document result. A signed external audit sink remains future work.

At this foundation checkpoint there was no production approval UI. Newer sections above add the host UI, semantic request, device-scoped citation review/grant lifecycle, and active revision-bound chat-context consumption. Research context remains separately unimplemented.

## Persistent Chat Semantic Cache

The runtime stores only candidate embeddings, never query embeddings. Each row is scoped by normalized owner id, session id, canonical provider-qualified embedding model id, a strong content-stable model fingerprint, and the exact bounded semantic-document fingerprint. Ollama persistence requires a canonical lowercase SHA-256 artifact digest; adapters without an immutable revision do not persist vectors.

Session event sequence is captured with the candidate snapshot and revalidated in the same SQLite write transaction. Appends and lifecycle mutations delete current rows, stale in-flight calculations cannot repopulate them, and connection cancellation is checked after store-lock acquisition and immediately before commit. Cache lookup is read-only, malformed rows are misses, failures degrade to on-demand embedding, and no vector, fingerprint, cache state, or model revision crosses the Android wire.

## Approved Memory Semantic Cache

Semantic `memory.list` considers only persisted, owner-scoped `RuntimeMemoryEntry` values. Generated review drafts and dismissed drafts are not candidates. Candidate documents contain only the approved entry content; source titles, excerpts, model ids, source pointers, and other audit metadata remain available for review but do not become embedding inputs. Query vectors are never persisted, and vectors, fingerprints, revisions, model ids, and cache state never cross the wire.

Persistent rows are held in an owner-only SQLite sidecar and bind owner, memory id, canonical provider-qualified model, strong content-stable model fingerprint, exact bounded-content fingerprint, and the current full approved-entry revision. Only canonical lowercase Ollama SHA-256 revisions enable persistence. Entry content, enabled state, timestamps, or source-audit changes rotate the revision. The runtime revalidates revisions after inference and under the memory-store lock before cache commit, while cancellation is checked after cache-lock acquisition and before commit.

Memory update and deletion synchronously purge the entry's derived rows before appending the mutation. A missing cache file is inert, but an existing cache that cannot be purged blocks the privacy-sensitive mutation instead of silently retaining derived data. Read or ordinary cache-write failures still degrade search to on-demand inference because exact document and source-revision keys prevent stale reuse. Android keeps queried results transient, ignores late or unsolicited request ids, and allows only unqueried refreshes to replace the complete persisted memory cache.

## Production P2P/NAT Security Design Review

The reviewed [production P2P/NAT security hardening portfolio](security-hardening/production-p2p-nat-v1/hardening.md) defines the security selection point before any concrete private-overlay connector. The current implementation remains limited to opaque expiring rendezvous material, route planning, injected connector and lifecycle seams, and encrypted relay fallback; candidate exchange, ICE/STUN/TURN, hole punching, and a production session handshake are not implemented.

The recommended candidate-control profile uses authenticated encrypted ICE signaling with pair-scoped short-lived credentials, candidate privacy filtering, paced connectivity checks, consent freshness, and authenticated TURN fallback. STUN reports reachability, TURN relays packets, and rendezvous carries opaque signaling; none establishes device trust or receives endpoint traffic keys. Candidate counts, destinations, retries, pending state, allocation lifetime, bytes, and bandwidth remain bounded before and after identity verification.

The recommended session profile is transport-neutral and binds both pinned device identities and roles, pair epoch, both ephemeral shares and session nonces, the complete candidate-exchange digest, selected path, protocol offer and selection, and relay lease digest before any runtime command. Direct-to-relay fallback and migration change only the validated path, never identity, encryption, protocol floor, or command authorization. Hole punching proves reachability, not identity. Trust removal and consent loss close current authority; replayed, expired, downgraded, partially validated, or development-plaintext paths fail closed.

This is a static no-device design review. It does not prove a concrete connector, STUN/TURN interoperability, NAT traversal, public-network reachability, path migration, latency, memory, battery, optical QR, physical Android, or production cryptography. The phone is disconnected.

## Development Relay Bounded Waiting And Authenticated Identity Fairness

Every unmatched room receives one monotonic deadline when its first peer enters the matcher. The default duration is `60` seconds, bounded to `1...3600` with no disable value, and the effective deadline is no later than the remaining allocation lease. Registration and readiness probes atomically remove an expired room under the matcher lock before matching, replacement, or visibility decisions, so delayed timer delivery cannot extend authority. A waiting result carries the same deadline out of that transaction instead of re-reading room state, so a concurrent counterpart match cannot be mistaken for deadline loss. Same-role replacement preserves the original live room deadline, so repeated reconnects cannot extend the wait. Match, disconnect, generation invalidation, replacement cleanup, timeout, and close release matcher-owned waiting state before the accepted source connection permit; an active bridge cancels both waiting monitors and is not throttled.

After runtime or paired-client relay admission cryptographically verifies a key, the matcher applies a role-separated quota keyed by the verified allocation-binding fingerprint. The default is `4` unmatched waits per authenticated identity across source addresses, configurable in `1...65536` with no disable value. Runtime identity comes from the revalidated binding after runtime proof, and paired-client identity comes from the pinned binding after client proof. Bootstrap clients without paired-client proof and explicit legacy peers never create an identity bucket; they remain bounded by global/source descriptor controls and the waiting deadline. Identity admission cannot refund, enlarge, or replace the pre-auth source permit.

Stable reasons are `waiting_peer_timed_out` and `authenticated_identity_waiting_quota_reached`. Saturating metrics cover identity waiting requests, admissions, quota rejections, timeouts, current authenticated waiters, and current authenticated identities without source, fingerprint, role, relay ID, token, lease, or proof labels. Multiple valid identities remain a Sybil path and shared NAT/VPN collateral remains at the pre-auth boundary. This is process-local development fairness, not production identity service, per-user isolation, public-network capacity proof, production TLS/KEX/pair-epoch implementation, or physical Android proof; the phone is disconnected.

## Development Relay Source Peer Quotas

Every accepted development-relay socket now acquires both the existing global connection permit and a permit keyed by the canonical accepted IPv4/IPv6 source. The default source connection quota is `64`; the default unmatched waiting-peer quota is `32`. Waiting peers count against both limits, and active bridge sockets keep their source connection permits through close. Established encrypted frame forwarding is not throttled or evicted by this admission control.

The matcher owns waiting quota transitions under the same lock as room publication. Normal admission leaves one global and one per-source slot available before the first waiter, and every waiting insertion atomically requires `connections + waiting + 1 <= limit` in both scopes before consuming another normal-admission slot. A socket admitted from that reserve is counterpart-only: an immediate opposite-role match or authenticated same-source waiting replacement confirms it, while probe/allocation, cross-source replacement, and new waiting-room use close it. Reserve provenance is retained so a per-source reserve candidate can discharge only a waiter owned by that source; global-only reserve remains source-agnostic. Same-source replacement is net-zero, and cross-source replacement checks the new source before releasing the original waiter when it uses normal admission. Match, replacement, disconnect, generation invalidation, candidate rejection, and close release their exact counts. Configuration has no disable value and requires `2 * waiting <= connections` to preserve counterpart headroom.

Stable reasons are `global_connection_limit_reached`, `source_connection_quota_reached`, `source_waiting_peer_quota_reached`, and `counterpart_candidate_not_matched`. Saturating metrics report only aggregate admission, rejection, counterpart candidate, current connection/waiting, and source counts. Rejection logs contain only the reason and global reason count, never a source address, relay ID, route/allocation token, or proof field.

Shared NAT/VPN users necessarily share quotas. Defaults `64/32` are configurable development-relay fairness guardrails, not per-user isolation, production capacity/load/latency validation, IPv6-prefix aggregation, public-relay protection, live-network proof, or production TLS/KEX/pair-epoch implementation. The phone is disconnected, so this is not physical Android proof.

## Development Relay Source-Aware Allocation Controls

Allocation- and renewal-prefixed control attempts are admitted through separate token buckets keyed from the accepted socket address before full request parsing. IPv4-mapped IPv6 canonicalizes to IPv4, native IPv6 includes its scope ID, and unrecognized families share one conservative unknown-source bucket. Refill uses a monotonic clock. One shared overflow bucket plus periodic idle cleanup enforces the configured hard cap without capacity eviction resetting exhausted buckets or a full-map scan on every request.

The default development policy allows preflight at `120/minute` with burst `30` and new allocation plus paired claim/renew at `30/minute` with burst `10`, retaining at most `4096` buckets for `15 minutes` idle. Stable global counters cover request, rejection, overflow, idle eviction, and tracked-bucket state and remain source-free. Logs do not include source addresses, route tokens, allocation tokens, relay IDs, or proof material.

Only the exact cheap strict preflight envelope can select the more permissive preflight bucket. Duplicate markers, mutation-like fields, malformed envelopes, and all renewals consume mutation capacity. Configuration validation also requires both bursts to fully refill within idle retention, so deleting an idle bucket cannot restore tokens sooner than monotonic refill.

The limit intentionally runs before allocation-token and signature work so invalidly authorized requests cannot force that work without spending source capacity. This also means shared NAT/VPN users share one bucket and can affect one another; it is a conservative development-relay tradeoff, not identity-level fairness. Probes, peer admission, waiting rooms, active bridges, and encrypted forwarding remain outside these rate buckets, while the separate source peer quotas above govern accepted connection and unmatched waiting admission.

This rate-limit evidence is separate from the source peer quota evidence above. Neither slice is production capacity/load/latency validation, public-relay protection, live-network proof, or implementation of the selection-gated production TLS/KEX/pair-epoch design. The phone is disconnected, so it is not physical Android proof.

## Development Relay Abuse-Control Foundation

The development relay now bounds every accepted socket with a lifetime permit that remains owned while the socket is parsing control records, waiting for its counterpart, or participating in an active bridge. Disconnected waiting peers are removed and release capacity. Every control record has one absolute monotonic read deadline and the existing 4096-byte framing limit; encrypted frame forwarding remains independent of that deadline.

Unauthenticated route-state probe is loopback-only by default. An exposed strict relay closes probe requests without reporting whether a route exists or a runtime waits, unless an operator explicitly selects `legacy-unauthenticated` for temporary physical diagnostics. Android treats a closed probe as unsupported and proceeds to the authenticated relay registration path; it does not treat that closure as route proof. Exposed unallocated legacy relay mode fails closed even when a bearer token exists.

This foundation is complemented by the bounded waiting, authenticated identity, source rate, and source peer quota controls above. It is not production TLS/KEX or public-relay abuse protection. The Android phone is disconnected, so it also provides no physical Android proof.

## Production Relay Security Design Review

The current implementation remains a development relay foundation. The reviewed [production relay security hardening portfolio](security-hardening/production-relay-v1/hardening.md) recommends two next controls, but neither is implemented yet: TLS 1.3 allocation with delegated service-signed lease capabilities, and a monotonic pair-epoch recovery state machine with deny-only emergency revocation and fresh-QR replacement.

Production endpoint key exchange must be peer-verifiable. Both endpoints must directly verify a canonical identity transcript containing both long-term identities, both ephemeral shares, both session nonces, pair epoch, lease generation, and the signed lease digest before installing traffic keys. Relay registration proofs remain defense in depth; the relay is not the trust terminator. Recovery mutations must be idempotent, close active and waiting rooms when revoked, and expose a read-only signed status operation that reconciles a committed transition when the final response is lost.

Implementation is selection-gated. The portfolio compares a private-overlay baseline, the recommended TLS/signed-lease design, and a split authority/relay design; it separately compares short leases, the recommended pair-epoch state machine, and deferred threshold recovery. The physical Android phone is disconnected, so this review contributes no physical, optical QR, public-network, or production-deployment proof.

The project is local-first: no cloud AI backend and no account-server requirement for model access. The runtime host owns execution and backend access. The client device is a controller/client. That split is the main security boundary. The current v0.1 implementation has desktop-runtime and mobile-client targets.

AetherLink may later use distributed rendezvous/bootstrap/DHT-style discovery, signaling, or an encrypted blind relay/TURN-style component for connectivity when paired devices are on different networks. That component is connection infrastructure only: it must not run models, proxy backend APIs in plaintext, inspect AI protocol payloads, store prompts/responses, see model lists, files, memory, or backend credentials, or become a cloud AI backend, account backend, or model-logic backend.

Current code status: remote P2P NAT traversal, decentralized/distributed rendezvous, production allocation identity/authorization, and production end-to-end transport deployment are not complete yet. Existing endpoint hints, Bonjour/mDNS records, USB reverse paths, localhost/dev-server flows, Android opaque `p2p_rendezvous` route persistence/planning/diagnostic refresh validation, and the outbound TCP development relay are development scaffolding behind the trusted-device boundary. QR-provisioned relay routes require pairwise route material including `relay_secret`, `relay_expires_at`, and `relay_nonce`; `relay_secret` plus `relay_nonce` derive the AES-GCM relay-frame key so AetherLink frame bodies are encrypted before relay forwarding and stale or mismatched QR route material can be rejected. Android P2P rendezvous records are stored only as complete opaque, expiring route material bound to the pinned runtime identity, whether they arrive through QR or explicitly enabled authenticated `route.refresh`; they are not trust authority, candidate exchange, decrypted payloads, STUN, hole punching, or a real P2P connector. Android product defaults do not advertise or automatically send authenticated `route.refresh`, so normal route repair stays latest-QR scan first. The development relay requires allocation by default, accepts only canonical `crypto=2` allocation, rejects unknown or expired relay ids, and can require an allocation token before it issues secret-free lease metadata. The runtime host generates or reuses the 32-byte traffic secret locally and delivers it only as endpoint route material; the allocation request, response, ticket store, and relay log do not contain it. The allocation token is only allocation gating and not device trust. For a stable relay id, non-advancing renewal attempts for the same relay id are ignored unless the lease expiry advances and the relay nonce changes; persisted allocation-store loading also skips malformed ticket entries and deduplicates duplicate relay ids with the same rule. Durable allocation registry operations and relay lifetime ownership now use separate byte ranges of one stable inode-bound transaction marker, so two cooperating local processes cannot split store, matcher, or active-room ownership. The macOS runtime host applies the same advancing lease rule before replacing saved same-relay bootstrap lease material that can feed QR generation or diagnostic route refresh. That is only a foundation slice, not the final production transport security model. Loopback, `.local`, link-local, and unspecified relay hosts are not QR-ready remote routes. Carrier-grade NAT, private IPv4, and ULA IPv6 relay literals are accepted only when QR generation explicitly opts into `relay_scope=private_overlay`, meaning a user-controlled VPN, tunnel, or private overlay makes the address reachable from both paired devices.

The current schema-v4 allocation registry supersedes the earlier lease-only, schema-v2 runtime-only, and schema-v3 global paired-room behavior: it stores authorization mode, pinned client ownership, ticket generation, and consumed-bootstrap tombstones; pair claim/renew uses generation compare-and-swap and fails closed on downgrade, key substitution, reuse, corrupt, duplicate, or unknown-version stores.

Strict allocated relay crypto v2 adds PSK-mixed ephemeral P-256 ECDH, fresh ordered client/runtime session nonces and public keys, canonical transcript confirmation, and paired-identity transport binding. Directional traffic secrets derive AES-GCM keys in 65,536-frame epochs; ordered replay, reordering, frame authentication failure, and counter exhaustion fail closed. If ephemeral private keys are not retained, later compromise of only the QR relay secret does not recover recorded session traffic. Endpoint-owned allocation v2 also prevents the default allocation-required relay from learning that PSK through allocation. The initial bootstrap caller is still runtime-only; only the first post-pairing claim and later renewals are co-authorized. This does not protect a leaked QR secret, provide post-compromise security or a complete unordered replay window, or turn the development relay into production end-to-end transport.

## Relay Allocation Cross-Process Ownership No-Device Gate

- One stable mode-`0600` marker contains fixed-format `U`/`A`/`E` state and a 64-character lowercase hexadecimal coordination token that is also stored in schema v4. POSIX `F_SETLK` byte range 0 serializes reload, binding/generation compare-and-swap, and persistence; byte range 1 is the relay lifetime owner lock.
- The process lock pool keys by marker inode, reuses its pooled descriptor, and retains only duplicates that cannot safely be closed because closing any descriptor for an `F_SETLK` file can release that process's locks. Blocking acquisition has a five-second monotonic deadline. Stale process snapshots therefore cannot erase unrelated allocations or wait forever for a cooperating owner.
- Pair claim updates remain indivisible: the bootstrap removal, deterministic pair binding, and consumed-bootstrap tombstone are encoded and atomically renamed together. Competing stale claims produce one winner and one conflict, and a stale create cannot restore a consumed bootstrap ID.
- Descriptor-relative `openat`, `fstatat`, `renameat`, and `unlinkat` operations use `O_NOFOLLOW` and require current-user-owned regular files with `nlink == 1` under a parent that is not group- or world-writable. File `fsync`, atomic rename, and directory `fsync` reconcile persistence.
- Missing established stores, dangling symlinks, hard links, case/path aliases, marker replacement, and coordination-token mismatch fail closed. A valid unversioned `rt1` store is recognized but all leases are revoked into an empty token-bound schema-v4 store because legacy identity cannot be migrated.
- Interrupted first initialization recovers only a token-matched schema-v4 store beside a `U` marker. A second process fails before independent matcher state, a concurrent same-instance `run()` cannot release the live owner, and bind failure releases byte range 1. Simultaneous first startup converges to one owner, and kernel release on exit permits a successor.
- Final evidence is 64 allocation tests, 21 socket tests, 100 related relay tests, 797 full Swift tests, a TCP-verified actual-process smoke, and the passing aggregate artifact `build/qa/check-no-device-quality-relay-cross-process-ownership-20260710.log` with 41 authenticated relay connections and 688 encrypted frame bodies. This is cooperative single-host advisory locking. It does not provide distributed consensus or protection against a privileged process that ignores the lock protocol. Allocation TLS/server authentication, service-signed responses, immediate revocation, P2P/NAT traversal, and physical Android proof remain unfinished. The phone is disconnected.

## Pair-Scoped Relay Room Isolation No-Device Gate

- `runtime-client-p256-v2` binds current and next relay IDs, both identities, both lease states, generation, challenge, and live transport binding. A claim must rotate to the deterministic pair-derived ID.
- Schema v4 commits the pair allocation and consumed-bootstrap tombstone together. Wrong key, replay, stale generation, bootstrap reuse, legacy mutation, duplicate ID, corrupt persistence, and unknown fields fail closed.
- `paired-client-p256-v1` proves the persistent Android client key before matcher admission. Invalid, missing, replayed, mutated, expired, or downgraded proofs cannot replace a verified waiting runtime.
- Active matcher rooms reject second runtime/client pairs until bridge release. Two different pair IDs can bridge concurrently, and loopback socket evidence verifies frames do not cross rooms.
- macOS keeps pair routes and relay clients per client fingerprint; secrets remain in the secret store. Removing a trusted device stops and removes that pair transport locally. Relay-side allocation invalidation is still lease-based and is not yet a signed revocation transaction.
- Android checks the deterministic pair ID before co-signing, stores the rotated ID/generation, and requires pre-ready admission on reconnect.
- This remains development security. Allocation TLS/server authentication, service-signed final responses, production abuse controls, P2P/NAT traversal, and physical different-network proof are unfinished.
- No physical Android, optical QR, or real different-network evidence was produced.

### Historical Runtime-Key Allocation Foundation

- Before paired claim/renew, a successful `rt2` allocation ID is key-bound to the verified runtime-key fingerprint.
- Allocation ticket schema v2 stores the runtime-key fingerprint and a generation, and persistence failure rejects allocation rather than returning unpersisted route material. Schema v4 preserves those invariants and migrates schema-v3 paired records into pair-scoped rooms.

## Local-First Threat Model

Assets to protect:

- Runtime host access.
- Local model prompts and responses.
- Runtime-host chat processing event logs, client-local UI cache, and runtime-owned user memory notes.
- Future compacted session summaries and embedding indexes.
- Future project files, project instructions, trusted-source settings, and project indexes.
- Future scheduled tasks, reminders, monitors, recurring automations, runtime-triggered jobs, client approvals, and audit logs.
- Trusted device identities.
- Future memory, files, image inputs, Python tool calls, MCP calls, and audit logs.

Primary threats:

- Any device on the same Wi-Fi attempts to control the runtime host.
- A malicious client sends protocol messages directly to the local transport.
- A paired client device is lost or should no longer be trusted.
- Runtime-host chat event logs, client-local UI cache, runtime-owned memory notes, compacted summaries, or embedding indexes leak from a device backup or filesystem.
- Project files or indexes are used as model/research context without project-scoped permission or trusted-source selection.
- A scheduled job runs later with broader file, network, tool, MCP, web search, or backend access than the user approved.
- Future tool execution gains file, terminal, network, or MCP access without explicit permission.
- Future image or file input flows bypass the runtime host and send user data directly to a serving backend.
- A future signaling/relay shortcut weakens the local-first guarantee by seeing prompts, responses, files, memory, model lists, or backend credentials.

v0.1 may use a small development transport while the product matures, but it must not normalize unauthenticated same-network runtime access, fixed IP/manual endpoint entry, or mDNS/local discovery as the product model.

## Trusted Devices

The target design is persistent device identity on both sides:

- The runtime host has a stable device id and keypair.
- The client device has a stable device id and keypair.
- The runtime host stores trusted client public keys.
- The client pins the trusted runtime identity.
- The runtime host exposes remove-trusted-device controls.

Pairing creates the persistent trust record used by later runtime sessions and by the connection manager. Removing a trusted device invalidates future authentication attempts from that client identity across local direct, remote P2P, and relay fallback paths.

The client identity must not silently rotate across app restarts. The client may persist its public device id and display name in the local preferences store, but private signing key material must remain in the platform key store. If the key store cannot load or create the signing key, the app should surface a device-identity failure instead of overwriting the previously persisted client id/name.

## Pairing Design

Target pairing flow:

1. User opens pairing on the runtime host.
2. The runtime host displays a QR code plus a one-time pairing code.
3. QR pairing data includes runtime device id, pairing nonce, service identity, runtime public key or certificate fingerprint, and a route token. For different-network product use, it also bootstraps private overlay/rendezvous/relay material. The current QR-provisioned relay path requires `relay_host`, `relay_port`, `relay_id`, `relay_secret`, `relay_expires_at`, and `relay_nonce`. A fixed host/port is not required for the product pairing payload and is only an optional development reachability hint.
4. The client submits pairing nonce, code, client identity, proof scheme, and a P-256 signature that binds both identities, the request id, and the active transport binding.
5. The runtime host verifies client-key possession, reserves the pairing request, signs the accepted result with its QR-pinned runtime key, and stores the client public key only after signing can succeed.
6. Future sessions use challenge-response authentication before runtime commands.

v0.1 persists the scanned trusted runtime record on the client only after the accepted `pairing.result` signature matches the pending request digest, QR-pinned runtime key/fingerprint, trusted client id, and current transport binding. The runtime verifies the client proof before trusted-device persistence and releases its pairing reservation if result signing or storage fails. The client connects to the runtime host, not to Ollama or LM Studio directly.

QR trust values must fail closed before pairing state is persisted. The client rejects whitespace-mutated pairing nonces, runtime ids, runtime fingerprints, runtime public keys, route tokens, relay ids, relay nonces, and relay scopes rather than trimming them into a different trusted identity or route. Runtime display names are normalized separately for UI, and relay frame secrets are treated as opaque secret material.

The runtime also validates submitted client identity before writing a trusted-device record. `device_id` and `public_key` are treated as opaque canonical values, the public key must decode as a P-256 DER public key compatible with the later challenge-response verifier, and display names are normalized separately. Invalid client identity material returns `pairing_invalid_device_identity` and leaves the trusted-device store unchanged.

Pending QR routes are stored with a stricter boundary than trusted route metadata. When a QR contains relay route material but pairing is not accepted yet, the client may keep the non-secret route metadata in its local runtime UI store so the app can retry after process recreation. The raw pending `relay_secret` must not be written into that JSON store. It is saved through the Android relay secret store and the pending route keeps only a deterministic secret reference. If that reference cannot be resolved on load, the pending pairing route is discarded and the user must scan a fresh QR. When a pending pairing route is cleared or replaced, the old secret-store reference must be removed as well so abandoned QR relay credentials do not linger locally.

An active pairing session allows only a bounded number of invalid nonce/code or identity-proof submissions. After the limit is reached, the runtime host invalidates that pairing session and returns structured `pairing.result` rejection details while preserving the existing `accepted: false` response shape. Rejected results do not create trust; Android keeps pending route recovery state when displaying an unsigned rejection.

## Initial Pairing Mutual Proof

- The Android device key signs a domain-separated, length-framed transcript containing protocol/scheme, request id, QR nonce/code, runtime id/key/fingerprint, client id/name/key/fingerprint, and the exact transport binding or `none`.
- The runtime verifies canonical P-256 DER keys, SHA-256 fingerprints, canonical DER signatures, scheme/version, and transport binding before reserving the pairing session.
- The runtime identity signs an accepted result containing the original request digest, runtime identity, trusted client id, generated message, and the same transport binding before either side treats pairing as complete.
- Android compares the result to the outstanding request and QR-pinned runtime key, verifies the runtime signature, and rechecks the live transport binding immediately before trusted-runtime persistence.
- Fixed Kotlin/Swift digest vectors, wrong-key, mutation, replay/request-id, downgrade, noncanonical Base64/DER, signer-failure reservation recovery, real relay TCP, and authenticated RuntimeDevServer smoke cover the no-device boundary.
- This is initial trust binding, not paired-device relay allocation authorization, post-compromise recovery, credential revocation propagation, production P2P traversal, or physical optical QR proof.

Implementation note: QR pairing and discovery can be simple in v0.1, but the docs must remain clear that the trusted-device gate is required before runtime commands execute.

Connectivity note: pairing is identity binding plus route bootstrap, not a promise that a scanned host/port will stay reachable. Product builds must keep pairing QR-only from the user's perspective while including enough private overlay material for different-network use: runtime identity, public key/fingerprint, route token, and rendezvous or relay allocation material. Raw local sockets, mDNS, or a remembered private IP cannot satisfy this requirement. The connection manager then resolves a route for that identity through local discovery/direct when available, remote P2P NAT traversal, or encrypted blind relay/TURN-style fallback.

NAT traversal note: the future remote path should use STUN-like address discovery and authenticated hole punching to attempt direct 1:1 connectivity. Any candidate exchange must be bound to paired device identities, short-lived rendezvous tokens, and replay protection before the encrypted AetherLink session is accepted. This is not implemented yet; current code only prepares route-candidate concepts around the paired identity.

Discovery note: Bonjour/local discovery candidates should carry minimal routing hints when possible. The preferred TXT hint is a pairing-derived `route_token`; stable device ids and public-key fingerprints are legacy/development fallback hints, not the production privacy target. Current macOS Bonjour TXT advertisement follows that boundary by publishing only the pairing-derived `route_token` identity hint and omitting stable runtime `device_id` and `fingerprint` values from local discovery TXT records. Whitespace-mutated `route_token` values are omitted instead of trimmed into discovery identity hints. Android Bonjour TXT receive canonicality mirrors that boundary by dropping discovered peers with whitespace-mutated, oversized, malformed UTF-8, or forbidden identity TXT material before trusted discovery matching can fall back to legacy metadata. The client must only auto-route a pinned trusted runtime identity to discovered LAN endpoints whose hints match the trusted runtime record. Endpoints without identity metadata, or with non-matching metadata, are local/dev/manual candidates only and must not be treated as automatically trusted.

Route-token lifecycle: production route tokens should be scoped to one trust relationship or pairing session, not to the whole runtime forever. Removing a trusted client should revoke that client's route token. Re-pairing should rotate the token. Local discovery should advertise only the minimum token needed for paired clients to recognize their runtime, and future remote rendezvous tokens should be short-lived values derived from paired-device secrets rather than stable public identifiers.

Privacy note: discovery identity hints are not an authorization layer and must stay minimal. They must not publish backend URLs, Ollama or LM Studio details, model inventory, provider health, prompts, responses, files, memory, or runtime command metadata. Pairing, pinned keys, challenge-response authentication, and encrypted transport remain mandatory before runtime commands execute.

Bitcoin-network analogy note: AetherLink should borrow only peer identity and discovery concepts. It is not a public open network; only QR-paired trusted devices may discover, authenticate, and communicate with the runtime.

DHT/bootstrap note: a future DHT-like or bootstrap-peer layer may be useful for finding a paired peer without a fixed IP, but it must publish only privacy-preserving rendezvous records. It must not expose stable runtime host directories, backend URLs, model inventory, prompts, files, memory, or any authority to mark a device as trusted.

Implementation status note: identity-only QR, local route-token matching, relay route preparation, macOS P2P QR generation, and the Android P2P rendezvous route-preparation/planning/restore contract are connection-manager increments. The P2P QR contract keeps opaque candidate records separate from relay aliases, but does not yet signal candidates, perform STUN, hole punching, or real NAT traversal. The temporary outbound TCP development relay uses strict crypto v2 for allocated routes: each endpoint contributes a fresh session nonce and ephemeral P-256 key, derives ECDH session material mixed with the QR relay secret, confirms the canonical transcript, and encrypts directional frames with epoch-derived AES-GCM keys. Legacy unallocated/plaintext diagnostics retain the old 3-token handshake and plain registered/ready lines. The allocation-required development endpoint can require a token, accepts only secret-free `crypto=2` bootstrap allocation, and supports paired P-256 claim/renew for one pinned client/runtime key pair. The runtime host owns and stores the bootstrap traffic secret, while runtime commands continue to require pairing, pinned runtime identity, and challenge-response authentication. Actual private per-user overlay routing, DHT/bootstrap discovery, NAT traversal, production signaling/service authentication, global room and multi-client isolation, immediate revocation, post-compromise recovery, and production end-to-end deployment are not complete.

Strict relay paired-identity transport binding note: Android and macOS bind crypto version, relay id, route nonce, ordered session nonces, and ordered ephemeral public keys into a canonical SHA-256 transcript. PSK-mixed ephemeral P-256 ECDH derives role-separated confirmation and traffic secrets; the confirmed binding is then included in v2 runtime-challenge and client-response signatures so authentication cannot be spliced onto another strict relay connection or downgraded to unbound v1. Local direct and legacy plaintext diagnostics remain on v1 because they have no confirmed transport binding. This provides forward secrecy against later compromise of only the relay secret when ephemeral private keys were not retained, and endpoint-owned allocation keeps that secret out of the default relay service. Paired allocation claim/renew now binds that exact authenticated transport to one pinned client/runtime key pair, but it does not cover a leaked endpoint/QR secret, post-compromise security, an unordered replay window, production allocation-service authentication/isolation, or complete production end-to-end deployment.

Development note: `AETHERLINK_DEV_PAIRING=1` is only for local automated smoke tests with RuntimeDevServer. It opens and prints a temporary pairing session for scripts, but runtime commands still require the normal pairing/trusted-device and challenge-response path. Do not enable this flag for production or normal trusted-device use.

## Session Authentication

After pairing, each runtime connection authenticates before model or command messages are accepted:

1. The client sends `hello` with its trusted device id.
2. The runtime host checks the trusted-device store and returns `auth.challenge` with a one-time nonce.
3. When the client has a pinned runtime public key from QR pairing, it verifies the runtime's challenge signature before sending a client signature.
4. The client signs the domain-separated message `AetherLink client auth response v1\n<device_id>\n<nonce>` with its paired private key and sends `auth.response`.
5. The runtime host verifies that exact message against the stored client public key. Raw nonce signatures are rejected to avoid cross-protocol signature reuse.
6. The runtime host allows runtime commands only after the connection is authenticated and the authenticated device id is still present in the trusted-device store.

Runtime commands include `runtime.health`, `models.list`, `models.pull`, `chat.send`, and `chat.cancel`. Requests sent before authentication fail with `authentication_required`; unknown or removed device ids fail with `pairing_required`; invalid client signatures fail with `authentication_failed`; invalid runtime proofs fail client-side with `runtime_authentication_failed`. Removing a trusted device must also revoke already-authenticated live sessions at the next command boundary: the runtime clears the cached auth session and returns `pairing_required` instead of letting stale connections continue.

The same authentication requirement applies on every transport. A relay or signaling server must not be trusted as an authenticator, must not terminate the end-to-end encrypted AetherLink session, and must not be able to forge either device identity.

## Runtime-Owned Local Storage

Runtime-owned chat processing logs, memory notes, trusted-device records, and fallback runtime identity files are sensitive local assets. The runtime stores these materials on the runtime host, not on a relay or model provider, and they must not be readable through group/world permissions. Current macOS chat history defaults to the SQLite/FTS runtime event store with legacy JSONL backfill; the SQLite database file is created or corrected to owner-only `0600`, its AetherLink support directory is created or corrected to `0700`, and legacy chat JSONL files use the same owner-only file policy during backfill-era coexistence. Current memory JSONL event logs are also created with owner-only `0600` permissions, their AetherLink support directory is created or corrected to `0700`, and each JSONL append reasserts file permissions so older broader-permission files are repaired lazily. The `trusted-devices.json` authorization store also creates or corrects its containing directory to `0700` and the trusted-device JSON file to `0600` before loading or after writing, without dropping valid trusted devices. The file-backed runtime identity fallback applies the same directory/file permission policy before loading or after writing, without rotating a valid existing identity key. The file-backed runtime identity fallback also signs nonce-bound auth challenges with the persisted public-key fingerprint, so fallback identity storage remains tied to the same challenge-response trust proof instead of becoming a fingerprint-only placeholder.

Runtime-owned chat history and memory are scoped to the authenticated trusted-device identity before they are listed, injected into `chat.send`, renamed, archived, restored, deleted, or title-generated. The client does not send an owner id in protocol payloads; the runtime derives `owner_device_id` from the authenticated connection and writes it to JSONL events. Legacy unscoped events remain readable only through unscoped/no-auth store calls and are intentionally not mixed into authenticated device views.

This is local filesystem hardening, not a replacement for future encrypted-at-rest storage, keychain-backed secrets, or user-controlled retention policy. Archived and deleted chat state remains runtime-owned and must not be used as memory, retrieval, research, or compaction input unless restored or explicitly selected by a future permissioned workflow.

## Remote Connectivity Security

See [connection-overlay.md](connection-overlay.md) for the phased connection-overlay design and the non-goals that keep remote discovery private, paired-device-only, and runtime-host-mediated.

Reliable different-network connectivity cannot be guaranteed with pure mDNS or local IP addressing. mDNS is local-link discovery, and private IPs usually do not route across NATs, mobile carriers, VPNs, or separate Wi-Fi networks.

Target security properties:

- The connection manager prefers local discovery and direct authenticated connections when both devices are reachable on the same network.
- Bonjour/local discovery records may advertise minimal runtime route hints for matching, preferably a pairing-derived `route_token`; the client only auto-routes to endpoints whose hints match the pinned trusted runtime identity.
- Metadata-less Bonjour/local endpoints are limited to local/dev/manual reachability candidates and are not trusted identity matches.
- Remote P2P NAT traversal uses paired identities, STUN-like address discovery, short-lived connection candidates, authenticated hole punching, and encrypted session establishment.
- Optional DHT/bootstrap-peer discovery exchanges only short-lived rendezvous records for paired identities and does not create public runtime access, accounts, backend routing, or model-logic hosting.
- Encrypted blind relay/TURN-style forwarding is a fallback for cases where direct P2P fails.
- Signaling and relay services see only connection metadata needed for reachability or opaque encrypted packets.
- End-to-end encryption is between the paired client device and runtime host, so relay infrastructure cannot read AI protocol payloads, model lists, prompts, responses, memory notes, files, or backend credentials.
- Fixed IP/manual host entry and mDNS/Bonjour local discovery are restricted to development, diagnostics, local fast paths, and emergency support flows, not normal onboarding.

Current implementation status: AetherLink has route-candidate plumbing for the target connection order, Android opaque P2P rendezvous QR/pending/trusted route planning, and a temporary outbound TCP development relay for different-Wi-Fi testing. Strict allocated relay crypto v2 uses ephemeral P-256 ECDH mixed with the QR relay secret, mutual transcript confirmation, paired-identity transport binding, directional traffic secrets, and 65,536-frame epochs. Secret-free allocation v2 leaves the traffic secret at the endpoints while the relay handles only lease metadata; schema-v3 paired claim/renew adds runtime/client P-256 authorization and generation continuity for one pinned pair. Local direct and legacy relay diagnostics remain unchanged. Different-network production P2P, NAT traversal, decentralized/bootstrap signaling, allocation-service TLS/server authentication, global isolation, immediate revocation, post-compromise recovery, and production end-to-end deployment remain future work.

Development preflight should fail closed for remote QR testing. `script/run_different_network_dev_runtime.sh --preflight-only` rejects accidental loopback, `.local`, unspecified, link-local, carrier-grade NAT, and private relay hosts unless an explicit private-overlay flag is used, and it checks the allocation API before RuntimeDevServer starts. When the relay requires an allocation token, preflight and RuntimeDevServer must send the same token with `--allocation-token` or `AETHERLINK_BOOTSTRAP_RELAY_ALLOCATION_TOKEN`. The app-side QR path blocks scope-less private IP relay literals and accepts them only when the QR marks `relay_scope=private_overlay`; the macOS GUI now requires an explicit private-overlay/VPN/tunnel opt-in before it emits that scope. This prevents a QR from looking remote-ready when the real blocker is the absence of reachable relay/bootstrap/overlay material.

Route-refresh handling must also fail closed. `route.refresh` responses may omit `relay_scope`, but any present value must match the protocol enum exactly: `remote`, `private_overlay`, or `usb_reverse`. The runtime must not emit unknown relay scopes, and the client must reject unknown or whitespace-mutated relay scopes before saving refreshed trusted route material. Relay fields and P2P fields are complete route-material families: partial, expired, or unsupported `p2p_rendezvous` records must be rejected before trusted storage just like malformed relay lease material. Authenticated relay refresh may reuse a stable relay id or frame secret, but it must advance the relay nonce and lease expiry before storage so an old relay lease cannot be replayed as a successful renewal.

Public access is forbidden for the same reason same-network unauthenticated access is forbidden: network reachability is not trust. A runtime host must never accept model, file, memory, tool, or backend commands just because a peer found it through local discovery, a bootstrap peer, a DHT-like record, a relay allocation, or a public address.

## Local Chat History, Memory, And Compaction

The runtime host stores chat processing events locally when it handles `chat.send`: request metadata, client-visible messages, streamed answer deltas, reasoning deltas, completion usage, cancellation, and errors. Inline attachment bytes are stripped before storage. Runtime-only system context is model-call input, not user-visible history; the capability guard and `Runtime user memory:` prompt context are filtered from stored and returned transcripts. The client may still keep a local UI cache for continuity, but user-managed memory notes are synchronized through the authenticated runtime host with `memory.list`, `memory.upsert`, and `memory.delete`. Current clients do not send memory notes as `chat.send` prompt context. Enabled memory notes are injected by the runtime from its own memory store before backend calls. Client-supplied cached memory context is compatibility input only; the runtime removes stale `Runtime user memory:` system messages and replaces them with its current enabled entries, or forwards no memory context if none are enabled. The client must not use memory features to call Ollama, LM Studio, web search, MCP, or tools directly.

Archive is not deletion. Archived chats remain retained, but they are excluded from memory, reflection, research, and compaction inputs unless the user explicitly restores them or selects them as sources. Deleted chats should be treated as removal requests, not hidden research material.

Runtime-host-side session storage, context-window compaction, embedding indexes, and deep-research-like research notebooks are sensitive data. A compacted summary, event log, or embedding index can still reveal private transcript content, so it must follow the same local-first storage, trusted-device, archive/delete, and future encryption rules as raw chat transcripts.

LLM-generated memory-summary review drafts bind the exact requested installed model and exact host-local `RuntimePromptSkillBinding` into cache lookup, concurrent generation identity, and publication-time revalidation. The generated event stores only canonical prompt-skill id/revision provenance, never the prompt body, and clients cannot supply or observe those fields. Legacy events are accepted only when both provenance keys are genuinely absent and map to a separately pinned immutable original-v1 binding. The shared strict JSON parser rejects duplicate and escaped-equivalent duplicate names before decoding, while explicit `null`, partial, malformed, or wrong-skill metadata is invalid. Generation also requires an explicit backend `done` event, stops at the first terminal event, and ignores post-terminal stream data; graceful exhaustion without `done` fails before publication or persistence. A 60-second whole-request deadline begins before installed-model discovery and ends when the source-current response is handed to `RuntimeMessageSink.send` for transport enqueue/publication intent; it does not include or prove peer receipt or end-to-end delivery. All answer plus explicit reasoning deltas share a 16,384-byte raw-output ceiling. Backend work uses an unpredictable host-private `memory-summary-generation-` identifier instead of a client request id. Timeout, last-waiter cancellation, and output overflow dispatch that exact id at most once through a concurrent asynchronous cancellation lane; the exact generation-key gate remains closed until both worker and cancellation completion, and one blocked cancellation cannot starve an unrelated model key. Each waiter owns its deadline, so one expired waiter cannot cancel a shared worker still owned by another. A one-shot request authority linearizes deadline expiry against source-current transport enqueue. JSONL holds its instance and cross-instance file locks, and SQLite holds `BEGIN IMMEDIATE`, from exact source recomputation through that enqueue call. The successful waiter first stores one candidate in a bounded in-process cache and invokes the sink inside the source-current transaction. Production local and relay sinks map completion to Network.framework `contentProcessed`; only a successful completion authorizes the concurrent conditional durable JSONL cache write. Each cache entry binds one materialized draft to one identity, and each publication owns a unique reservation ID. Publication or persistence in flight pins that identity across same-draft replacement and capacity cleanup, duplicate completion cannot release another waiter, and a cache-derived worker result can reserve only its exact still-present identity rather than reinsert a superseded snapshot. Insertion evicts only unpinned entries and fails closed when all 256 slots are pinned, retaining the hard bound. The first successful completion starts persistence. One concurrent or later success can request one retry only when the store declares generated-draft caching idempotent; consuming that retry permanently closes the identity's budget at two total store calls, while failure remains terminal for a non-idempotent store. Every newly materialized generation carries a canonical host-private UUID persistence operation id. JSONL deduplicates only the same operation id under the cross-instance lock, rejects the same id with conflicting data, and appends a distinct operation even when its value and second-rounded generation time equal a historical event. The current generated event for a draft is selected by physical JSONL append ordinal captured before timestamp sorting, so a wall-clock rollback cannot make an older operation current after reopen. Legacy rows without an operation id retain exact-value retry compatibility. The operation id is stored only in the host JSONL event and is absent from protocol and Android models. Persisted identities stay in the bounded cache long enough for already-registered shared-flight waiters, then remain evictable. Durable persistence remains outside the source transaction and 60-second deadline, cannot delay the response path or connection close, and may be pending while immediate review or approval uses the in-process candidate. A failed transport completion leaves the bounded materialized candidate available for a later successful completion without another backend call. Timeout or disconnect before enqueue sends nothing and schedules no durable write; a superseded identity can persist only its own exact delivered draft and can never acquire a replacement token. A changed model or current revision cannot reuse the old result, and an unavailable historical definition or incomplete stream is not overlaid during listing or approval, which leaves the deterministic visible-source preview as the fail-closed review path.

Publication visibility is stricter than materialization. Only successful transport completion marks the exact in-process identity as review-visible; list and explicit approval may use that published identity while persistence is pending, but a failed candidate is retry-only and cannot be listed or approved. Android sends the exact displayed preview and summary method only after opt-in challenge capability negotiation. The new client advertises `auth.challenge.runtime_capabilities.v1`; the runtime then returns `memory.summary.approval_method.v1`. Without that response, Android omits `expected_summary_method`, preserving a new-client-to-old-runtime approval path, while a new runtime omits `runtime_capabilities` for an older client. The host accepts explicit approval only when supplied bindings uniquely match the recomputed deterministic preview or a current-source published/persisted generated candidate; byte-identical deterministic and generated text is therefore not attributed to the wrong method. Omitted-content compatibility can use only a durable generated event and otherwise falls back to the deterministic preview. A same-operation A retry remains one event after B, while a newly generated A with a distinct operation id appends and becomes current even when all three events share a second-rounded timestamp.

Android memory-summary list and decision state is also authority-bound, not request-id-only. List, generate, approve, and dismiss correlations capture the exact request id, channel, connection generation, and authenticated authority generation. A generated result may use canonical `llm_summary_v1`; generated and approved source identity excludes only naturally changing `inactive_seconds`, while draft id, summary method where present, session id/title/model/last activity/message count, source message count/range, and every source-pointer field remain exact. Approval additionally requires canonical enabled-entry state and exact generated content. Once an approve/dismiss result passes correlation, malformed, unknown-metadata, or otherwise noncanonical data is terminal: the client consumes the correlation, clears the action UI, drains one deferred authoritative refresh, and ignores a later canonical result carrying the old request id. Reconciliation requested behind a pending action is retained and drains after the final success, stale, malformed, protocol-error, or send-failure path; revocation, reauthentication, replacement, disconnect, or receive failure clears both correlations and retained reconciliation before new authority can publish or persist state. Closed correlated errors reject unknown metadata before authentication handling. Namespaced client request ids keep arbitrarily delayed errors out of generic state handling after the bounded tombstone window without creating an action, permission, or trust grant.

The current no-device evidence for this lifecycle includes 54 Swift memory-summary checks, sixteen generated-draft store checks, seven direct transport completion checks, one conditional capability-negotiation router check, targeted Android protocol/ViewModel compatibility checks, all 1,475 current-source Swift package tests, and the refreshed default aggregate. It validates source-current enqueue, Network.framework `contentProcessed`-gated publication/persistence, operation-id idempotence/conflict handling, physical append selection under clock rollback, and negotiated exact displayed method approval with mocks and loopback transport; it does not validate peer receipt, physical Android, optical QR, live-provider behavior, external networking, or production P2P/NAT behavior.

LLM-generated chat-compaction summaries bind the exact current `chat_compaction_summary_v1` identifier and revision before cache lookup, prepass dispatch, incremental prefix reuse, and commit. The prompt body remains registry-owned, enters only the backend-only prepass system message, and is absent from SQLite, runtime events, transcripts, FTS, protocol payloads, and Android state. Exact and prefix cache scopes include the binding, while a derived pre-binding schema is dropped instead of attributed to the current prompt. A missing or drifted current definition performs no cache read and no LLM prepass; ordinary chat continues with the deterministic bounded preview. Derived-prepass registration is fenced by the same runtime-owned terminal state as primary dispatch, so cancellation before registration prevents dispatch and cancellation during registration forces the returned generation to be cancelled before any primary call. A generated row must revalidate the same current binding before and during the transactional cache commit.

The host-local compaction calibration report validates only a bounded newest window: JSONL reads backward under existing coordination with 64 MiB, 50,000-line, and 4 MiB-record ceilings, while SQLite examines at most 50,000 terminal rows and resolves at most 1,000 indexed normalized-owner plus exact-session/request bindings. It processes the newest 1,000 fully eligible terminal records first, caps output groups at 32, and emits only exact provider/model/wire/estimator keys plus bounded relation counts. Deterministic previews remain request-estimate-equal; duplicate terminals, malformed or wrong-type calibration, missing bindings, and incomplete bounded scans fail closed. A 20-sample `ready_for_review` floor means only that a group is ready for human review; one hard input-budget exceedance always forces the warning state. Loading occurs off the main actor, and failed refreshes clear stale content. Report output excludes prompt/message bodies, summary text, owner/session/request/event identifiers, timestamps, and source pointers. It is absent from the wire schema, Android, search indexes, provider calls, and automatic compaction policy, so this no-device surface neither creates an exfiltration path nor turns post-dispatch evidence into authorization or provider-tokenizer parity.

Durable research notebooks also bind the exact host-local prompt-skill identifier and SHA-256 revision that created them. The SQLite store persists only that canonical binding, never the prompt body, and schema-v4 migration stamps the then-current byte-identical research definition only after validating the complete legacy notebook, grant, lifecycle-intent, and foreign-key state in one transaction. A corrupt migration rolls back without a partial schema advance.

Follow-up requests resolve the notebook's stored binding before approved-source consumption, chat persistence, or backend dispatch. Retained historical definitions preserve prior notebook behavior; a missing or retired definition fails closed with `runtime_prompt_skill_unavailable`. The binding is revalidated again at commit and remains host-local metadata: it is not a client-selected permission, protocol field, notebook-list field, or authority to use files, processes, Python, terminals, sockets, networks, MCP, web search, or additional providers.

Embedding models must be selected separately from chat/text-generation models. Future retrieval, ranking, and knowledge indexing use the selected embedding model, and indexed sources must respect archive/delete state.

Short model inactivity and long memory inactivity are separate policies. The runtime may unload the active model after the persisted host-local 5, 10, or 30 minute idle policy elapses; 10 minutes is the default. The preference stores only the bounded policy enum, is enforced by the runtime host, and grants no client or provider authority. Host updates are serialized, switch unload finishes before new-model admission, same-model provider dispatch waits for an active unload, and cancelled waiters are rejected before residency admission. Provider-confirmed model unload does not trust HTTP success alone: Ollama requires a terminal unload acknowledgement and absence from `/api/ps`, while LM Studio requires exact native model/instance identity, exact acknowledgement, and observed absence. Missing, null, duplicate, unsupported, or persistently resident state fails closed; raw provider errors remain host-local and generic localized errors are sanitized. Longer inactivity criteria can later create compact memory summaries, but archived sessions stay excluded unless the user restores or selects them.

## Future Files, Images, And Python Tools

Future file inputs and image inputs must be mediated by the runtime host. The client can provide UI for choosing or approving inputs, but the client must not send those inputs directly to Ollama, LM Studio, future serving backends, or indexing services.

Future internal Python execution is a runtime-host tool for deterministic tasks such as calculations. The runtime host must own permissions, filesystem/network scope, audit logs, and result reporting. The client remains the approval and display surface.

The closed `runtime_python_sandbox_v1_recommended` packet is the current review boundary and remains `proposed_not_selected`. It rejects in-process subinterpreters and plain system-Python `Process` execution as containment, and instead recommends a separately signed one-shot XPC worker with its own minimum-privilege App Sandbox and a pinned signed embedded CPython artifact loaded before source. The worker accepts one operation, launches no interpreter child, and exits; Python retains zero child-process authority. App Sandbox is not claimed to enforce executable identity after native compromise, so same-process `exec` remains residual risk under the same containment, ceilings, and bounded untrusted-result authority. The proposed profile has no files, network, packages, native extensions, shared Keychain/App Group, security-scoped bookmarks, or ambient host descriptors. A pinned `-I -S -B` equivalent, syntax restrictions, audit hooks, and resource limits are layered controls only. Future source rejects bidi controls/default ignorables before token-aware display. Every run binds exact authority, execution closure, worker code/signing/entitlements, profile, interpreter, source, input, and limit digests to host-local approval. Approval binds expected closure before durable reservation; after worker start, XPC audit-token identity is checked before untrusted handoff and result acceptance, and cancellation/drift terminates only that worker instance. Execution and termination/cleanup use separate deadlines, and success audit binds worker identity, canonical result, and publication-envelope digests before publication. The packet authorizes none of this implementation or execution and static no-device validation is not sandbox-escape evidence.

## Future Project And Automation Security

Project/workspace features are permissioned runtime features, not v0.1 client-only state. Project-scoped chats, files, instructions, memories, indexes, model/backend preferences, trusted-source settings, and project-level search/research must be owned by the runtime-host boundary. Client devices can select sources, request searches, approve actions, and display status, but they must not call file indexers, research tools, Ollama, LM Studio, future serving backends, MCP, or web search directly.

Scheduled tasks, reminders, monitors, recurring automations, and runtime-triggered jobs are also permissioned runtime actions. They can run when the user is not actively watching the UI, so each job definition must store a narrow permission scope, require explicit approval for sensitive capabilities, and write audit records for creation, edits, approvals, execution, failures, cancellations, and permission changes.

Project files and scheduled jobs are sensitive by default. A scheduled job that reads project files, uses project indexes, calls a model backend, runs Python or terminal work, performs web search, or invokes MCP must pass through the same runtime-host-side permission broker as an interactive request. Client approval/status surfaces should be treated as controllers only; approval does not move execution to the client.

## Encryption Roadmap

v0.1 development transport:

- Local network socket with length-prefixed JSON.
- Clear runtime module boundary so transport can be replaced.
- No direct backend URL exposed to the client.
- Runtime commands rejected until the device is trusted and authenticated.

Hardening roadmap:

- Promote the strict relay v2 and paired claim/renew foundation through a reviewed production key-exchange and allocation design; preserve endpoint-owned traffic secrets while adding service authentication, global room/multi-client isolation, rotation, immediate revocation, and recovery.
- Define recovery, post-compromise security, traffic-secret replacement, and replay policy beyond ordered per-connection counters and derived 65,536-frame epochs.
- TLS or Noise-style encrypted channel between the client device and runtime host.
- Runtime identity pinning on the client.
- Keychain-backed runtime public key and fingerprint confirmation during QR pairing.
- Client public-key challenge-response on the runtime host.
- Session keys derived after pairing.
- Protocol commands rejected until authenticated.
- Connection manager with local direct, remote P2P NAT traversal, and encrypted blind relay/TURN-style fallback.
- Optional BLE/QR/mDNS only for discovery, pairing, development reachability hints, or key exchange, not as the only product connectivity plan.

## Same-Network Unauthenticated Access Is Forbidden

Same Wi-Fi is not a trust boundary. A guest device, compromised laptop, or any local network process could otherwise send `chat.send`, list models, or later trigger tools.

Therefore:

- Runtime commands must require a trusted-device check and successful challenge-response authentication.
- Ollama remains bound to the runtime host adapter and is never exposed as the client API. The client must not call Ollama `/api/tags`, `/api/ps`, `/api/pull`, `/api/chat`, or LM Studio endpoints directly.
- Model installation is also a runtime command: the client may request `models.pull`, but only the authenticated runtime host may call Ollama `/api/pull`.
- Future file, image, Python, terminal, MCP, skills, and web search actions must pass through a runtime-host-side permission broker and audit log.
