# libnice offline source intake v3

The one-shot `decision-v4` acquisition is consumed. The exact official libnice 0.1.23 archive and detached signature were received without redirects or environment proxies. The 469,330-byte archive has SHA-256 `618fc4e8de393b719b1641c1d8eec01826d4d39d15ade92679d221c7f5e4e70d`.

Safe extraction produced 184 regular files totaling 2,197,866 bytes. Every extracted file matches the archive; there are no links, special files, duplicate paths, or traversal entries. The deterministic source-tree digest is `e594b0b2435e10a8df970304ba3dec24ea0353820f1eecb820a810ab56cd276a`.

The detached signature bytes are pinned, but cryptographic signature trust is not claimed because no local OpenPGP verifier and trusted signing key were available. Official HTTPS colocation is not treated as signature verification.

The direct dependency closure is partial: GLib 2.64.2 is exactly locked for the next bounded intake; OpenSSL remains outside the currently effective GLib-family acquisition scope. No compiler, build system, source generator, socket, runtime network, Phase B, or production action was performed or authorized.
