# libnice offline source intake v4

The libnice 0.1.23 and GLib 2.64.2 one-shot acquisition authorities are consumed. Their exact archive and extracted-tree identities are preserved by the committed manifests, and no additional acquisition remains open.

The completed static audit rejects `libnice-0.1.23-glib-c-abi` before compilation for four independent P1 production-profile failures. The remaining libffi, GNU libiconv, proxy-libintl, and OpenSSL source intakes were therefore skipped. Their proposed versions are dependency-planning evidence, not acquired or hash-verified source evidence.

The libnice detached signature bytes remain pinned without a trusted-key OpenPGP verification result. No source or generator, configure step, build system, compiler, static archiver, linker, test, socket, runtime network, Phase B, or production action was executed.

Any further candidate, source acquisition, source fork, or compile work requires a new versioned networking-library review and explicit authority.
