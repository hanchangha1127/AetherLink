# Bounded dependency source identity and acquisition decision Wave3 v2

This successor decision preserves the exact Wave3 v1 graph, tuple order,
parent declarations, selected flags, and version-specific vertices. It binds
the completed one-use SumDB execution and its separate consumed offline
readback. The independently read-back identity for
`github.com/kr/pty@v1.1.1` is the held `go.mod` H1
`h1:pFQYn66WHrOpPYNljwOMqo10TkYh1fy3cYio2l3bCsQ=` paired with module ZIP H1
`h1:VkoXIwSboBpnk99O/KFauAEILuNHv5DVFKZMBN/gUgw=`.

Wave3 therefore has 16 `go.mod` identities, 16 ZIP identities, 16 complete
pairs, and 32 of 32 required checksum identity records. No tuple is blocked,
dropped, rejected, replaced, or reordered. This is checksum-identity
readiness only: it is not source, author, repository, or release attestation.

This decision is not an execution permit. It does not authorize network
access, filesystem mutation, source acquisition, archive extraction, source
loading or execution, compilation, package-manager use, subprocesses, Git,
devices, deployment, authentication, credentials, keys, tokens, passwords,
signatures, or user action. No dependency fixed point, source review, semantic
closure, candidate selection, library selection, Rung Three completion, or
release readiness is established.

The only next action is to prepare a separate one-use Wave3 source-acquisition
permit, checker, runner, and tests for the exact 16-tuple, 32-resource ordered
`.mod` and `.zip` request set. That later permit must independently bound its
network and write authority and require post-consumption readback.
