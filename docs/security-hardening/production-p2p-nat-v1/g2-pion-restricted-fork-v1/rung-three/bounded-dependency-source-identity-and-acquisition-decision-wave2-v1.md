# G2 Pion dependency source wave2 identity decision v1

## Outcome

The exact 15-tuple frontier from the completed wave1 v3 graph review and its
independent readback is preserved as a separate wave2 intake. Each tuple expands
to exactly two future resources in `.mod` then `.zip` order, for 30 requests.

The future resource identities are anchored to already-held module-root
`go.sum` evidence. Every tuple has one non-conflicting `.mod` H1 and `.zip` H1
pair. The decision also binds each tuple to the exact parent `.mod` declaration
that introduced that version-specific graph vertex.

## Boundary

This is preparation only. It does not authorize networking, acquisition,
filesystem mutation, archive extraction, package-manager use, compilation,
source loading, source execution, review continuation, deployment, or Git
operations. Future response bytes have not yet been acquired or verified
against the bound H1 values.

The held `go.sum` lines are local identity inputs; they are not claimed as a
fresh checksum-database inclusion proof. The five MVS-selected tuples and ten
non-selected version-specific tuples remain distinct. Lower versions are not
deduplicated into selected versions.

No repository account, owner proof, login, key, signature, token, password, or
user authentication is required for this personal-project preparation step.
Product endpoint authentication remains a separate runtime invariant and is not
evaluated here.

## Next step

Prepare a separate fail-closed wave2 runner, mutation tests, and one-use
execution permit. Any future run must use the fresh `wave-2-v1` namespace,
request only the 30 ordered resources, verify each response against its bound
H1 before acceptance, publish receipts in order, and require an independent
readback before combining wave1 and wave2 for another fixed-point graph pass.
